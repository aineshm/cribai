---
phase: 03-semantic-search
verified: 2026-03-06T04:15:00Z
status: passed
score: 4/4 must-haves verified
---

# Phase 3: Semantic Search Verification Report

**Phase Goal:** CribAI understands qualitative preferences and ranks listings by semantic relevance, not just SQL filters
**Verified:** 2026-03-06T04:15:00Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Listings have vector embeddings generated by Gemini and stored as pgvector columns | VERIFIED | Migration `006_pgvector_embeddings.sql` creates `extensions.vector(768)` column with HNSW index (m=16, ef_construction=64). `generate-embedding.ts` calls Gemini `gemini-embedding-001` with `RETRIEVAL_DOCUMENT` task type and 768 dimensions. `embed-listings.ts` orchestrates batch embedding with change detection (`updated_at > last_embedded_at`). |
| 2 | User asking CribAI for "quiet place near campus with natural light" gets semantically relevant results | VERIFIED | `search-listings.ts` detects `semantic_query` parameter, calls `generateQueryEmbedding()` with `RETRIEVAL_QUERY` task type, then invokes `match_listings_semantic` RPC which returns results ordered by cosine similarity (`1 - (embedding <=> query_embedding)`). Schema description explicitly guides LLM to set `semantic_query` for qualitative preferences. |
| 3 | Hard filters (price, bedrooms) still work alongside semantic ranking in hybrid search | VERIFIED | `match_listings_semantic` RPC accepts `p_bedrooms`, `p_min_rent`, `p_max_rent`, `p_min_fairness` and applies them as WHERE clauses before similarity ranking. `search-listings.ts` passes these from the input schema. SQL-only fallback path preserved when `semantic_query` is absent (backward compatible). |
| 4 | CribAI can display search results on an interactive map within the chat | VERIFIED | `MapBlock` type in `chat.ts` discriminated union. `search-listings.ts` returns `mapBlock` for 3+ results with lat/lng. `cribai.ts` emits `mapBlock` as separate SSE event. `chat-block-renderer.tsx` handles `case 'map'` with `next/dynamic` lazy-loaded `ChatMapBlock`. Map uses `react-map-gl/mapbox` with price-label pins, popup cards, and highlighted selected pin. |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/006_pgvector_embeddings.sql` | pgvector extension, embedding column, HNSW index, RPC function | VERIFIED | 98 lines. CREATE EXTENSION vector, ALTER TABLE with vector(768), HNSW index, match_listings_semantic RPC with hybrid filtering, updated_at trigger. |
| `packages/ai/src/embeddings/synthesize-text.ts` | Listing text synthesis for embedding | VERIFIED | 113 lines. Exports `synthesizeListingText`. Neighborhood context from Madison streets, vibe derivation from amenities, null-safe field handling. |
| `packages/ai/src/embeddings/generate-embedding.ts` | Gemini embedContent wrapper | VERIFIED | 71 lines. Exports `generateEmbedding` (RETRIEVAL_DOCUMENT) and `generateQueryEmbedding` (RETRIEVAL_QUERY). 768 dimensions. Handles old/new API shapes. |
| `packages/ai/src/embeddings/embed-listings.ts` | Batch orchestrator for embedding pipeline | VERIFIED | 104 lines. Exports `embedChangedListings`. Change detection via `last_embedded_at.is.null,updated_at.gt.last_embedded_at`. Sequential processing. Returns metrics. |
| `packages/ai/src/embeddings/index.ts` | Barrel exports | VERIFIED | 3 lines. Exports all three modules. |
| `packages/ai/src/tools/handlers/search-listings.ts` | Hybrid search: SQL filters + vector similarity | VERIFIED | 253 lines. Semantic path calls RPC, SQL fallback path preserved. Map block for 3+ results. No numeric scores in modelContext. |
| `packages/ai/src/tools/schemas.ts` | Updated FunctionDeclaration with semantic_query | VERIFIED | `semantic_query` property with description. `relevance` added to sort enum. |
| `packages/ai/src/tools/types.ts` | ToolResult with optional mapBlock | VERIFIED | `mapBlock?: ChatBlock` field added. |
| `packages/ai/src/cribai.ts` | Emits mapBlock as separate SSE event | VERIFIED | Conditional `result.mapBlock` check and emission after main clientBlock. |
| `packages/ai/src/cli/embed.ts` | CLI entry point for GH Actions | VERIFIED | 50 lines. Creates Supabase client, calls embedChangedListings, outputs metrics for GH Actions. |
| `apps/web/components/chat/chat-map-block.tsx` | Interactive Mapbox map with price-label pins | VERIFIED | 88 lines. react-map-gl/mapbox Map, Marker with price pills, Popup with ChatMapPopup, highlighted selected pin (blue vs white). |
| `apps/web/components/chat/chat-map-popup.tsx` | Pin popup card with listing preview | VERIFIED | 45 lines. Photo, address, rent, beds/baths, "View details" link. |
| `apps/web/components/chat/chat-block-renderer.tsx` | Updated renderer with 'map' case | VERIFIED | `case 'map'` with `next/dynamic` lazy-loaded ChatMapBlock (ssr: false). |
| `.github/workflows/nightly-scrape.yml` | Post-scrape embedding generation step | VERIFIED | "Generate embeddings for changed listings" step gated on success(), uses `npx tsx packages/ai/src/cli/embed.ts`, metrics output for summary. |
| `packages/types/src/chat.ts` | MapBlock in discriminated union | VERIFIED | `mapBlockSchema` and `mapListingSchema` defined. `mapBlockSchema` included in `chatBlockSchema` union. `MapBlock` and `MapListing` types exported. |
| `packages/types/src/listing.ts` | Embedding fields | VERIFIED | `embeddingText` and `lastEmbeddedAt` optional nullable fields added. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `embed-listings.ts` | `synthesize-text.ts` | `import synthesizeListingText` | WIRED | Line 9: `import { synthesizeListingText } from './synthesize-text'` and used at line 67. |
| `embed-listings.ts` | `generate-embedding.ts` | `import generateEmbedding` | WIRED | Line 10: `import { generateEmbedding } from './generate-embedding'` and used at line 77. |
| `search-listings.ts` | `generate-embedding.ts` | `import generateQueryEmbedding` | WIRED | Line 4: `import { generateQueryEmbedding } from '../../embeddings/generate-embedding'` and called at line 54. |
| `search-listings.ts` | `match_listings_semantic RPC` | `context.supabase.rpc` | WIRED | Line 56: `context.supabase.rpc('match_listings_semantic', {...})` with all parameters. |
| `chat-block-renderer.tsx` | `chat-map-block.tsx` | `dynamic import ssr:false` | WIRED | Lines 11-19: `dynamic(() => import('./chat-map-block')...)` with `ssr: false`. |
| `chat-map-block.tsx` | `react-map-gl/mapbox` | `Map, Marker, Popup imports` | WIRED | Line 4: `import { Map, Marker, Popup } from 'react-map-gl/mapbox'` and all three used in JSX. |
| `chat-map-block.tsx` | `@campusnest/types MapBlock` | type import | WIRED | Line 6: `import type { MapBlock } from '@campusnest/types'` and used in props interface. |
| `cribai.ts` | `ToolResult.mapBlock` | SSE emission | WIRED | Lines 158-159: `if (result.mapBlock) { yield {...result.mapBlock} }` |
| `nightly-scrape.yml` | `embed-listings.ts` | CLI embed step | WIRED | Step runs `npx tsx packages/ai/src/cli/embed.ts` which imports `embedChangedListings`. |
| `migration SQL` | `listings table` | `ALTER TABLE` | WIRED | `ALTER TABLE listings ADD COLUMN embedding extensions.vector(768)` with HNSW index. |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| SRCH-01 | 03-01 | Listings embedded with Gemini embedding-001 and stored as pgvector columns | SATISFIED | Migration creates vector(768) column with HNSW index. Pipeline generates embeddings via Gemini embedding-001 with RETRIEVAL_DOCUMENT task type. |
| SRCH-02 | 03-02 | CribAI performs hybrid search combining vector similarity with SQL filters | SATISFIED | `search-listings.ts` semantic path calls RPC with hard filters (bedrooms, rent, fairness) alongside vector similarity ranking. SQL-only fallback preserved. |
| SRCH-03 | 03-03 | CribAI displays listings on interactive map as chat block | SATISFIED | `ChatMapBlock` renders Mapbox map with price-label pins and popup cards. Lazy-loaded via `next/dynamic` (ssr: false). Integrated into `ChatBlockRenderer` switch. |
| SRCH-04 | 03-02 | Search results ranked by semantic relevance to user's NL query | SATISFIED | `match_listings_semantic` RPC orders by `embedding <=> query_embedding` (cosine distance). Results arrive pre-sorted by similarity. |

No orphaned requirements found -- all 4 SRCH requirements mapped to Phase 3 in REQUIREMENTS.md are covered by plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | - | - | - | No TODO, FIXME, placeholder, or stub patterns found in any phase 3 files |

### Human Verification Required

### 1. Semantic Search Quality

**Test:** Ask CribAI "quiet place near campus with natural light" and verify results are qualitatively relevant (not just keyword matches).
**Expected:** Results should favor listings with amenities/descriptions suggesting quietness and natural light, ranked by semantic similarity.
**Why human:** Embedding quality and semantic relevance cannot be verified without actual Gemini API calls and real listing data.

### 2. Map Rendering

**Test:** Trigger a semantic search returning 3+ results and verify the map block renders in the chat.
**Expected:** Interactive Mapbox map with price-label pins at correct locations, popup on click with photo/address/rent/beds/baths and "View details" link.
**Why human:** Visual rendering, Mapbox token configuration, and map interactivity require browser testing.

### 3. Nightly Pipeline End-to-End

**Test:** Run the nightly scrape workflow and verify the embedding generation step executes after fairness recalculation.
**Expected:** Embedding step processes changed listings, logs metrics, and writes summary to GitHub Actions job summary.
**Why human:** Requires GitHub Actions execution with GEMINI_API_KEY secret configured.

### Gaps Summary

No gaps found. All 4 success criteria are verified through artifact existence, substantive implementation, and complete wiring. The embedding pipeline, hybrid search handler, map UI components, and CI integration are all fully implemented and connected.

Tests cover 711 lines across 5 test files (synthesize-text, generate-embedding, embed-listings, search-listings-semantic, map-block). No anti-patterns detected. All key links verified as WIRED.

---

_Verified: 2026-03-06T04:15:00Z_
_Verifier: Claude (gsd-verifier)_
