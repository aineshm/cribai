---
phase: 01-auth-and-platform-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - supabase/migrations/004_profile_student_fields.sql
  - packages/types/src/profile.ts
  - apps/web/components/profile-form.tsx
  - apps/web/app/settings/profile/page.tsx
  - apps/web/app/settings/layout.tsx
  - apps/web/components/profile-modal.tsx
  - apps/web/app/(campus)/[campusSlug]/layout.tsx
autonomous: false
requirements: [AUTH-04, AUTH-05]

must_haves:
  truths:
    - "After first successful auth, user sees a modal overlay prompting profile completion with a visible skip button"
    - "User can skip profile creation and the modal does not reappear on subsequent page loads"
    - "User can fill in display name, avatar initials, university, graduation year, and major in the profile form"
    - "User can edit their profile at any time from a persistent settings/profile page"
    - "Profile changes are saved to the database and reflected immediately in the UI"
  artifacts:
    - path: "supabase/migrations/004_profile_student_fields.sql"
      provides: "Student context columns on profiles table"
      contains: "avatar_url"
    - path: "packages/types/src/profile.ts"
      provides: "Updated Zod profile schema with student fields"
      contains: "graduationYear"
    - path: "apps/web/components/profile-form.tsx"
      provides: "Shared profile form used by both modal and settings page"
      contains: "graduation_year"
    - path: "apps/web/app/settings/profile/page.tsx"
      provides: "Settings page for profile editing"
      contains: "profile-form"
    - path: "apps/web/components/profile-modal.tsx"
      provides: "First-login profile completion modal with skip button"
      contains: "skip"
  key_links:
    - from: "apps/web/components/profile-form.tsx"
      to: "profiles table"
      via: "supabase update query"
      pattern: "supabase.*from.*profiles.*update"
    - from: "apps/web/components/profile-modal.tsx"
      to: "apps/web/components/profile-form.tsx"
      via: "renders ProfileForm component"
      pattern: "ProfileForm"
    - from: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      to: "apps/web/components/profile-modal.tsx"
      via: "renders modal when profile incomplete and not dismissed"
      pattern: "ProfileModal"
    - from: "apps/web/app/settings/profile/page.tsx"
      to: "apps/web/components/profile-form.tsx"
      via: "renders ProfileForm with existing profile data"
      pattern: "ProfileForm"
---

<objective>
Build the profile system: database migration for student fields, shared profile form component, settings page for editing, and first-login modal with skip button.

Purpose: Users need to optionally complete a profile with student context (display name, university, graduation year, major) and be able to edit it later from a settings page. The student fields also prepare for v2 roommate matching.
Output: Working profile creation/editing flow with first-login prompt and persistent settings page.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-platform-foundation/01-RESEARCH.md
@.planning/phases/01-auth-and-platform-foundation/01-01-SUMMARY.md
@.planning/phases/01-auth-and-platform-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing database schema from 001_initial_schema.sql -->
profiles table (current columns):
  - id: uuid PRIMARY KEY REFERENCES auth.users(id)
  - campus_id: uuid REFERENCES campus_configs(id)
  - display_name: text
  - edu_email: text
  - is_edu_verified: boolean NOT NULL DEFAULT false
  - verification_status: text NOT NULL DEFAULT 'unverified'
  - subscription_tier: text NOT NULL DEFAULT 'free'
  - stripe_customer_id: text
  - created_at: timestamptz DEFAULT now()
  Note: Auto-created via trigger on auth.users insert (empty fields)

<!-- Current Zod profile schema from packages/types/src/profile.ts -->
```typescript
export const profileSchema = z.object({
  id: z.string().uuid(),
  campusId: z.string().uuid().nullable(),
  displayName: z.string().nullable(),
  eduEmail: z.string().email().nullable(),
  isEduVerified: z.boolean().default(false),
  verificationStatus: verificationStatusSchema.default('unverified'),
  subscriptionTier: subscriptionTierSchema.default('free'),
  stripeCustomerId: z.string().nullable().default(null),
  createdAt: z.string().datetime().optional(),
});
export type Profile = z.infer<typeof profileSchema>;
```

<!-- From Plan 01-02: campus layout already queries user profile -->
From apps/web/app/(campus)/[campusSlug]/layout.tsx:
  - Already queries profiles table for is_edu_verified
  - Gets user via supabase.auth.getUser()
  - Passes user data to AuthNav component

<!-- From Plan 01-02: AuthNav already has settings link -->
From apps/web/components/auth-nav.tsx:
  - Has Settings link pointing to /settings/profile (added in Plan 02)

<!-- Supabase client factories -->
From packages/supabase/src/client.ts:
  - `createClient()` for browser-side Supabase client

From packages/supabase/src/server.ts:
  - `createServerComponentClient(cookieStore)` for server component Supabase client
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile migration and updated Zod schema</name>
  <files>
    supabase/migrations/004_profile_student_fields.sql,
    packages/types/src/profile.ts
  </files>
  <action>
    1. Create `supabase/migrations/004_profile_student_fields.sql`:
       ```sql
       ALTER TABLE profiles
         ADD COLUMN IF NOT EXISTS avatar_url text,
         ADD COLUMN IF NOT EXISTS graduation_year smallint
           CHECK (graduation_year BETWEEN 2020 AND 2035),
         ADD COLUMN IF NOT EXISTS major text,
         ADD COLUMN IF NOT EXISTS profile_completed_at timestamptz;
       ```
       The `profile_completed_at` timestamp tracks when user first completed their profile (null = never completed). This serves double duty: showing completion state AND preventing the modal from reappearing.

    2. Update `packages/types/src/profile.ts`:
       - Add new fields to profileSchema:
         - `avatarUrl: z.string().url().nullable().default(null)`
         - `graduationYear: z.number().int().min(2020).max(2035).nullable().default(null)`
         - `major: z.string().max(200).nullable().default(null)`
         - `profileCompletedAt: z.string().datetime().nullable().default(null)`
       - Keep all existing fields unchanged
       - Create a `profileFormSchema` for form validation (subset of fields users can edit):
         - `displayName: z.string().min(1, 'Display name is required').max(100)`
         - `graduationYear: z.number().int().min(2020).max(2035).optional()`
         - `major: z.string().max(200).optional()`
       - Export both schemas and their inferred types
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && cat supabase/migrations/004_profile_student_fields.sql && pnpm --filter @campusnest/types exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Migration adds avatar_url, graduation_year, major, profile_completed_at to profiles
    - Zod schema includes new fields + profileFormSchema for form validation
    - Types package compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Profile form component, settings page, and settings layout</name>
  <files>
    apps/web/components/profile-form.tsx,
    apps/web/app/settings/profile/page.tsx,
    apps/web/app/settings/layout.tsx
  </files>
  <action>
    1. Create `apps/web/components/profile-form.tsx`:
       - Client component ('use client')
       - Props interface: `{ readonly initialData: { displayName: string | null; graduationYear: number | null; major: string | null; avatarUrl: string | null }; readonly onSuccess?: () => void; readonly submitLabel?: string }`
       - Renders form fields:
         - Display name: text input (required)
         - Avatar: initials display derived from display name (read-only visual, no upload for Phase 1). Show a colored circle with initials computed from the display name. Keep `avatar_url` column for future upload support.
         - University: read-only text showing "University of Wisconsin-Madison" (campus-scoped, not editable)
         - Graduation year: select dropdown with years 2024-2035
         - Major: text input (optional)
       - On submit:
         - Validate using `profileFormSchema.safeParse()`
         - Call `supabase.from('profiles').update({ display_name, graduation_year, major, profile_completed_at: new Date().toISOString() }).eq('id', user.id)`
         - Use `createClient()` from `@campusnest/supabase/client`
         - Get current user via `supabase.auth.getUser()`
         - Show success toast via sonner on save
         - Show error toast on failure
         - Call `onSuccess?.()` callback after successful save
       - Handle loading state during save
       - Use Tailwind styling consistent with existing design tokens (var(--primary-*), var(--surface-*))

    2. Create `apps/web/app/settings/layout.tsx`:
       - Server component
       - Simple layout with nav back to campus page and a heading "Settings"
       - Protect route: check user via `getUser()`, redirect to `/login` if not authenticated
       - Use consistent styling with campus layout (max-w-6xl, padding)

    3. Create `apps/web/app/settings/profile/page.tsx`:
       - Server component that loads current profile data from Supabase
       - Query: `supabase.from('profiles').select('display_name, avatar_url, graduation_year, major').eq('id', user.id).single()`
       - Pass profile data to `<ProfileForm initialData={...} submitLabel="Save changes" />`
       - Page title: "Edit Profile"
       - If no user session, redirect to /login
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ProfileForm component renders all fields with validation
    - Settings page loads profile and renders ProfileForm
    - Settings layout protects route and provides navigation
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 3: Profile completion modal with skip logic</name>
  <files>
    apps/web/components/profile-modal.tsx,
    apps/web/app/(campus)/[campusSlug]/layout.tsx
  </files>
  <action>
    1. Create `apps/web/components/profile-modal.tsx`:
       - Client component ('use client')
       - Props: `{ readonly initialData: { displayName: string | null; graduationYear: number | null; major: string | null; avatarUrl: string | null }; readonly isProfileIncomplete: boolean }`
       - State: `isOpen` initialized from a useEffect that checks both `isProfileIncomplete` prop AND localStorage key `profile_modal_dismissed`
       - Renders a modal overlay (fixed position, z-50, semi-transparent backdrop) containing:
         - Heading: "Complete your profile" with friendly subtitle
         - The shared `<ProfileForm>` component with `onSuccess={() => setIsOpen(false)}` and `submitLabel="Save profile"`
         - A clearly visible "Skip for now" button below the form. On click: set `localStorage.setItem('profile_modal_dismissed', 'true')` then `setIsOpen(false)`.
       - Check localStorage on mount: if `profile_modal_dismissed` is 'true', don't show even if `isProfileIncomplete`
       - When profile is saved successfully via ProfileForm's onSuccess, the `profile_completed_at` gets set in the DB (handled by ProfileForm's update query), so modal won't show on future page loads even without localStorage.
       - Modal should be dismissible by clicking backdrop (same as skip)
       - Smooth fade-in animation via Tailwind: `animate-fade-in` or transition classes

    2. Update `apps/web/app/(campus)/[campusSlug]/layout.tsx`:
       - Expand the existing profile query to also select `display_name, avatar_url, graduation_year, major, profile_completed_at` (currently only selects `is_edu_verified`)
       - Determine `isProfileIncomplete`: `!profile?.display_name && !profile?.profile_completed_at`
         - This means: show modal if user has no display_name AND hasn't completed/been prompted before
       - If user exists and `isProfileIncomplete` is true, render `<ProfileModal>` with the profile data
       - Import ProfileModal component
       - Pass `initialData` from the profile query
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ProfileModal component exists with form and skip button
    - Skip sets localStorage flag to prevent modal reappearing
    - Completing profile sets profile_completed_at in DB (prevents modal on future loads)
    - Campus layout queries full profile data and conditionally renders modal
    - Modal does not show if profile_completed_at is set OR localStorage flag is set
    - TypeScript compiles
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify complete auth and profile flow end-to-end</name>
  <files>none</files>
  <action>
    Human verification of the complete Phase 1 flow. All automated work is done in Plans 01-01, 01-02, and Tasks 1-3 of this plan.

    Steps to verify:
    1. Visit the root URL — should redirect to /uw-madison/cribai
    2. Try entering a non-.edu email on the login page — should see inline error
    3. Enter a valid .edu email — should see "Check your email" confirmation
    4. Click the magic link in your email — should land on /uw-madison/cribai
    5. Profile completion modal should appear with skip button
    6. Click "Skip for now" — modal closes
    7. Refresh the page — modal should NOT reappear
    8. Navigate to /settings/profile — should see the profile form
    9. Fill in display name, graduation year, major — save
    10. Check mobile: resize browser to phone width — nav should collapse to hamburger
    11. Navigate to dashboard page — should see placeholder sections

    Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web exec tsc --noEmit && pnpm --filter @campusnest/web test 2>&1 | tail -20</automated>
  </verify>
  <done>
    User has verified the end-to-end flow and typed "approved".
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @campusnest/web exec tsc --noEmit` passes
- `pnpm --filter @campusnest/web test` passes
- Profile modal shows on first login, skip works
- Settings page loads and saves profile
- Mobile layout is functional
- Dashboard shell page renders with placeholder sections
</verification>

<success_criteria>
- Profile completion modal appears exactly once for new users with visible skip
- Profile can be edited from /settings/profile at any time
- Student context fields (display name, graduation year, major) save correctly
- Modal does not reappear after skip or profile completion
- Entire auth + platform flow works end-to-end on desktop and mobile
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-platform-foundation/01-03-SUMMARY.md`
</output>
