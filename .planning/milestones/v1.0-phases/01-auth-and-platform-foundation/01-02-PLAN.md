---
phase: 01-auth-and-platform-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/003_uw_madison_seed.sql
  - apps/web/app/page.tsx
  - apps/web/app/(campus)/[campusSlug]/layout.tsx
  - apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx
  - apps/web/components/auth-nav.tsx
  - apps/web/components/mobile-nav.tsx
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03]

must_haves:
  truths:
    - "User arriving at root URL sees the UW Madison campus experience"
    - "Navigation is usable on mobile screens with a hamburger menu"
    - "Chat interface works on mobile without nav overlapping the input area"
    - "Multi-campus architecture supports additional campuses via campusSlug routing"
    - "Authenticated user can navigate to a dashboard page from the nav"
  artifacts:
    - path: "supabase/migrations/003_uw_madison_seed.sql"
      provides: "UW Madison seed data in campus_configs table"
      contains: "uw-madison"
    - path: "apps/web/app/page.tsx"
      provides: "Root redirect to default campus"
      contains: "uw-madison"
    - path: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      provides: "Mobile-responsive campus layout with hamburger nav"
      contains: "dvh"
    - path: "apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx"
      provides: "Dashboard shell page with placeholder sections"
      contains: "dashboard"
    - path: "apps/web/components/auth-nav.tsx"
      provides: "Auth nav with settings link for authenticated users"
      contains: "settings"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "/uw-madison/cribai"
      via: "redirect()"
      pattern: "redirect.*uw-madison"
    - from: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      to: "campus_configs table"
      via: "supabase query by slug"
      pattern: "campus_configs.*slug"
    - from: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      to: "/[campusSlug]/dashboard"
      via: "nav link in layout"
      pattern: "dashboard"
---

<objective>
Establish UW Madison as the primary campus with seed data, set up root redirect, create dashboard shell page, and make the layout responsive for mobile browsers.

Purpose: Platform needs a real campus to function, mobile users need a usable interface, the multi-campus architecture (already built) needs verification, and the dashboard page structure needs to exist per user decision.
Output: UW Madison seed data, root redirect, dashboard shell, mobile-responsive nav and chat layout.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-platform-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs to modify -->

From apps/web/app/(campus)/[campusSlug]/layout.tsx:
- Server component that queries campus_configs by slug
- Maps snake_case DB fields to CampusConfig camelCase type
- Gets user via getUser(), checks is_edu_verified from profiles
- Has sticky nav with Links to listings, cribai, and AuthNav component
- Nav is NOT mobile-responsive (no hamburger menu, flex row only)
- Main content uses `max-w-6xl px-6 py-8`

From apps/web/components/auth-nav.tsx:
```typescript
interface AuthNavProps {
  readonly userEmail: string | null;
  readonly isEduVerified: boolean;
}
```
- Shows "Sign in" link when no user
- Shows email + sign out button when authenticated
- Has "Verify .edu" link when not verified
- Missing: link to settings/profile page

From apps/web/app/page.tsx:
- Current root page (needs to be replaced with redirect)

From supabase/migrations/001_initial_schema.sql:
- campus_configs table exists with columns: id, slug, name, university_name, edu_domains (text[]), latitude, longitude, timezone, scrape_cron, scrape_radius_km, config (jsonb), is_public, created_at
- profiles table auto-created via trigger on signup

From apps/web/middleware.ts:
- Remembers last_campus cookie on /*/cribai visits
- Protects /*/cribai routes (requires auth)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UW Madison seed data and root redirect</name>
  <files>
    supabase/migrations/003_uw_madison_seed.sql,
    apps/web/app/page.tsx
  </files>
  <action>
    1. Create `supabase/migrations/003_uw_madison_seed.sql`:
       - INSERT INTO campus_configs with ON CONFLICT DO NOTHING (idempotent):
         - slug: 'uw-madison'
         - name: 'UW-Madison'
         - university_name: 'University of Wisconsin-Madison'
         - edu_domains: ARRAY['wisc.edu'] (the primary .edu domain)
         - latitude: 43.0766 (UW-Madison campus center)
         - longitude: -89.4125
         - timezone: 'America/Chicago'
         - scrape_cron: '0 3 * * *' (3 AM CT daily)
         - scrape_radius_km: 8 (covers campus and near-campus housing)
         - config: '{}' (empty for now)
         - is_public: true
       - Use a deterministic UUID for the id (e.g., gen_random_uuid() or a fixed UUID constant) so it's reproducible. Use `ON CONFLICT (slug) DO NOTHING` for idempotency.

    2. Replace `apps/web/app/page.tsx`:
       - Import `redirect` from `next/navigation`
       - Export default server component function that calls `redirect('/uw-madison/cribai')`
       - This makes the root URL go directly to the CribAI chat on UW Madison (chat-first per user decision)
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Migration file exists with UW Madison seed data
    - Root page.tsx redirects to /uw-madison/cribai
    - TypeScript compiles
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile-responsive nav, dashboard shell, and multi-campus verification</name>
  <files>
    apps/web/app/(campus)/[campusSlug]/layout.tsx,
    apps/web/components/auth-nav.tsx,
    apps/web/components/mobile-nav.tsx,
    apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx
  </files>
  <action>
    1. Create `apps/web/components/mobile-nav.tsx`:
       - Client component ('use client')
       - Hamburger button (three horizontal lines icon using divs or SVG) visible only on small screens: `md:hidden`
       - When open, renders a slide-down or overlay menu with the same links as desktop nav (Listings, CribAI, Dashboard, AuthNav, Settings link)
       - When closed, only the hamburger icon shows
       - Use useState for open/close toggle
       - Close menu on link click (use onClick or usePathname to detect navigation)
       - Props: `{ campusSlug: string; userEmail: string | null; isEduVerified: boolean }`

    2. Update `apps/web/app/(campus)/[campusSlug]/layout.tsx`:
       - Import MobileNav component
       - Make the desktop nav links (Listings, CribAI, Dashboard, AuthNav) hidden on small screens: `hidden md:flex`
       - Add MobileNav component visible on small screens: renders the hamburger
       - Add "Dashboard" link to the desktop nav links alongside Listings and CribAI, pointing to `/${campusSlug}/dashboard`
       - Change the main content container to use `min-h-[calc(100dvh-64px)]` (subtract nav height) so chat can fill available height on mobile. Use `dvh` units (dynamic viewport height) to handle mobile browser URL bar.
       - Keep the existing server component pattern (MobileNav is a client component child receiving props)

    3. Update `apps/web/components/auth-nav.tsx`:
       - Add a "Settings" link next to the email display for authenticated users: `<Link href="/settings/profile">Settings</Link>` styled as a small text link
       - This prepares for Plan 03 which builds the settings page

    4. Create `apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx`:
       - Server component
       - Protect route: check user via `getUser()`, redirect to `/login` if not authenticated
       - Page title: "Dashboard"
       - Render three placeholder sections as empty cards with headings:
         - "Upcoming Appointments" with text "No appointments yet"
         - "Recently Viewed" with text "No recently viewed listings"
         - "Saved Items" with text "No saved items yet"
       - Use Tailwind grid layout (responsive: 1 col on mobile, 2-3 cols on larger screens)
       - Each card should use the project's design tokens (var(--surface-*), var(--primary-*))
       - This is a shell page — actual functionality (saved listings, appointments) comes in Phase 4+

    5. Verify PLAT-02 (multi-campus architecture): The existing `[campusSlug]` dynamic routing + campus_configs table query + CampusProvider context already supports multiple campuses. Add an automated verification step that confirms a different campus slug (e.g., 'test-campus') would resolve through the same route handler — check that the layout.tsx queries campus_configs by the dynamic slug param (not hardcoded to 'uw-madison'). Document in SUMMARY.
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web exec tsc --noEmit 2>&1 | tail -10 && grep -q "campusSlug" apps/web/app/\(campus\)/\[campusSlug\]/layout.tsx && echo "PLAT-02: campusSlug dynamic routing confirmed"</automated>
  </verify>
  <done>
    - Hamburger nav component exists and renders on small screens
    - Desktop nav links hidden on mobile, shown on md+
    - Dashboard link appears in nav (desktop and mobile)
    - Dashboard shell page exists at /[campusSlug]/dashboard with placeholder sections
    - Campus layout uses dvh for mobile viewport handling
    - AuthNav includes Settings link for authenticated users
    - PLAT-02 verified: layout queries by dynamic campusSlug param, not hardcoded
    - TypeScript compiles
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @campusnest/web exec tsc --noEmit` passes
- `supabase/migrations/003_uw_madison_seed.sql` exists with valid SQL
- `apps/web/app/page.tsx` contains redirect to /uw-madison/cribai
- `apps/web/components/mobile-nav.tsx` exists
- `apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx` exists with placeholder sections
- Campus layout uses dvh units
- AuthNav has settings link
- Layout uses dynamic campusSlug param (PLAT-02 multi-campus support)
</verification>

<success_criteria>
- UW Madison is seeded as primary campus with correct coordinates and edu_domains
- Root URL redirects to UW Madison CribAI chat (chat-first experience)
- Navigation collapses to hamburger menu on mobile
- Chat layout handles mobile viewport correctly with dvh
- Dashboard shell page exists with placeholder sections for appointments, recently viewed, and saved items
- Multi-campus architecture verified as supporting additional campuses via dynamic slug routing
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-platform-foundation/01-02-SUMMARY.md`
</output>
