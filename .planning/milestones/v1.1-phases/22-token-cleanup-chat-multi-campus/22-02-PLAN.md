---
phase: 22-token-cleanup-chat-multi-campus
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/chat/ChatProvider.tsx
  - apps/web/app/(campus)/[campusSlug]/layout.tsx
  - apps/web/components/chat/__tests__/ChatProvider.test.tsx
autonomous: true
requirements:
  - EXPL-04
must_haves:
  truths:
    - "ChatProvider accepts a campusSlug prop (not hardcoded)"
    - "Campus layout passes the real campusSlug to ChatProvider"
    - "CribAI API calls from campus routes carry the correct campusSlug in the request body"
    - "Explore page chat still works (ChatProvider in root layout covers it with empty string default)"
    - "All ChatProvider unit tests pass"
  artifacts:
    - path: "apps/web/components/chat/ChatProvider.tsx"
      provides: "campusSlug prop interface with '' default"
      exports: ["ChatProvider", "useChatContext"]
    - path: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      provides: "ChatProvider mounted with real slug from route params"
    - path: "apps/web/components/chat/__tests__/ChatProvider.test.tsx"
      provides: "Updated test asserting prop-injected campusSlug (not hardcoded 'uw-madison')"
  key_links:
    - from: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      to: "apps/web/components/chat/ChatProvider.tsx"
      via: "campusSlug prop passed from server component params"
      pattern: "ChatProvider campusSlug="
    - from: "apps/web/components/chat/ChatProvider.tsx"
      to: "/api/ai/cribai"
      via: "fetch POST body includes campusSlug from prop"
      pattern: "campusSlug"
---

<objective>
Make `ChatProvider` campus-aware by accepting a `campusSlug` prop instead of hardcoding `'uw-madison'`, then inject the real slug from the campus layout's route params.

Purpose: EXPL-04 requires the CribAI chat panel to work correctly as a floating slide-over. The current hardcoded `'uw-madison'` means any campus other than UW Madison would receive wrong context. This was a deliberate Phase 18 deferral; Phase 22 closes it.

Output: Updated ChatProvider (prop interface), updated campus layout (injects slug), updated test (asserts injected value).
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-token-cleanup-chat-multi-campus/22-RESEARCH.md
@.planning/phases/22-token-cleanup-chat-multi-campus/22-VALIDATION.md
</context>

<interfaces>
<!-- Extracted from direct codebase inspection. No exploration needed. -->

Current ChatProvider signature (apps/web/components/chat/ChatProvider.tsx, line 27):
```typescript
export function ChatProvider({ children }: { readonly children: ReactNode }) {
```

Line 58 — the hardcoded value to fix:
```typescript
campusSlug: 'uw-madison',
```

Current root layout mount (apps/web/app/layout.tsx, lines 33-35):
```tsx
<ChatProvider>
  {children}
</ChatProvider>
```

Campus layout — campusSlug available from params (apps/web/app/(campus)/[campusSlug]/layout.tsx, line 21):
```typescript
const { campusSlug } = await params;
```

The campus layout already wraps children in:
```tsx
<CampusProvider campus={campusConfig}>
  <ConciergeShell>
    ...children...
  </ConciergeShell>
</CampusProvider>
```

ChatProvider is NOT currently in the campus layout — it is only in apps/web/app/layout.tsx (root).

Explore page (apps/web/app/(main)/explore/page.tsx) uses:
- AIChatButton — calls useChatContext()
- AIChatPanel — calls useChatContext()
The explore page is under (main), NOT under (campus)/[campusSlug].
ChatProvider must remain in root layout to cover the explore page.

Existing test file (apps/web/components/chat/__tests__/ChatProvider.test.tsx):
- Line 98: `expect(callBody.campusSlug).toBe('uw-madison');` — this MUST be updated
- All renders use `<ChatProvider>` with no props — these need `campusSlug="test-campus"` added to the relevant test
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update ChatProvider to accept campusSlug prop, update test</name>
  <files>
    apps/web/components/chat/ChatProvider.tsx,
    apps/web/components/chat/__tests__/ChatProvider.test.tsx
  </files>
  <behavior>
    - Test: ChatProvider with campusSlug="test-campus" prop sends "test-campus" in API body (not 'uw-madison')
    - Test: ChatProvider with no campusSlug prop sends "" (empty string) in API body
    - Test: All existing tests continue to pass (initial state, SSE streaming, error handling, empty message)
  </behavior>
  <action>
    RED phase — update the test first:

    In `apps/web/components/chat/__tests__/ChatProvider.test.tsx`:
    1. Find the test "sendMessage POSTs to /api/ai/cribai with correct body" (lines 72-99)
    2. Change the render to: `<ChatProvider campusSlug="test-campus">` (add the prop)
    3. Update line 98: change `expect(callBody.campusSlug).toBe('uw-madison')` to `expect(callBody.campusSlug).toBe('test-campus')`
    4. Add a new test "sends empty campusSlug when no prop provided" — render `<ChatProvider>` without prop, assert `callBody.campusSlug` equals `''`

    Run `pnpm --filter web test -- --run components/chat/__tests__/ChatProvider.test.tsx` — tests for campusSlug should fail (RED).

    GREEN phase — update ChatProvider.tsx:

    In `apps/web/components/chat/ChatProvider.tsx`:
    1. Update the props interface:
    ```typescript
    interface ChatProviderProps {
      readonly children: ReactNode;
      readonly campusSlug?: string;
    }
    ```
    2. Update the function signature:
    ```typescript
    export function ChatProvider({ children, campusSlug = '' }: ChatProviderProps) {
    ```
    3. Update line 58 — replace the hardcoded string:
    ```typescript
    campusSlug: campusSlug,
    ```
    4. The `sendMessage` useCallback must capture `campusSlug` from the outer scope. Add `campusSlug` to the useCallback dependency array: `}, [campusSlug]);`

    Run tests — all should pass (GREEN).

    Do NOT use useCampus() inside ChatProvider. Do NOT read from usePathname(). The prop is the explicit, testable interface. Rationale from research: ChatProvider is above the CampusProvider tree at root layout; useCampus() would throw outside campus routes.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run components/chat/__tests__/ChatProvider.test.tsx</automated>
  </verify>
  <done>All ChatProvider tests pass. campusSlug prop accepted. Prop value appears in API call body. Empty string default when prop omitted.</done>
</task>

<task type="auto">
  <name>Task 2: Inject campusSlug into ChatProvider from campus layout</name>
  <files>
    apps/web/app/(campus)/[campusSlug]/layout.tsx,
    apps/web/app/layout.tsx
  </files>
  <action>
    The campus layout (apps/web/app/(campus)/[campusSlug]/layout.tsx) already extracts `campusSlug` from params at line 21. The ChatProvider is currently only in the root layout with no campusSlug prop.

    Strategy: Add a second ChatProvider mount inside the campus layout that wraps the campus content with the real slug. The root layout ChatProvider (with '' default) remains to cover the explore page and other (main) routes.

    In `apps/web/app/(campus)/[campusSlug]/layout.tsx`:
    1. Add import: `import { ChatProvider } from '@/components/chat/ChatProvider';`
    2. Wrap the existing `<CampusProvider campus={campusConfig}>` tree with `<ChatProvider campusSlug={campusSlug}>`:
    ```tsx
    return (
      <ChatProvider campusSlug={campusSlug}>
        <CampusProvider campus={campusConfig}>
          <ConciergeShell>
            ...existing content...
          </ConciergeShell>
        </CampusProvider>
      </ChatProvider>
    );
    ```

    This is safe because React context resolution is innermost-wins: components inside the campus layout tree will use the campus-scoped ChatProvider (with real slug). Components outside (explore page, etc.) use the root layout ChatProvider (empty string default).

    In `apps/web/app/layout.tsx`: no changes required. The root ChatProvider stays as-is with no campusSlug prop (defaults to '').

    After making the campus layout change, run the full test suite to verify no regressions:
    `pnpm --filter web test -- --run`
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run</automated>
  </verify>
  <done>Campus layout wraps content with ChatProvider campusSlug={campusSlug}. Root layout ChatProvider unchanged. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web test -- --run components/chat/__tests__/ChatProvider.test.tsx` — all tests green
2. `pnpm --filter web test -- --run` — full suite green
3. `pnpm --filter web build` — build passes with zero errors
4. Manual: grep for hardcoded 'uw-madison' string in ChatProvider.tsx returns zero results
   `grep "uw-madison" apps/web/components/chat/ChatProvider.tsx` — should return nothing
</verification>

<success_criteria>
- ChatProvider accepts optional campusSlug prop (string, defaults to '')
- Campus layout passes real campusSlug from route params to ChatProvider
- CribAI API calls from campus routes include the correct campusSlug
- Explore page chat continues to work (root layout ChatProvider covers it)
- All ChatProvider unit tests pass with updated assertions
- No hardcoded 'uw-madison' remains in ChatProvider.tsx
- EXPL-04 satisfied: CribAI slide-over panel sends correct campus context
</success_criteria>

<output>
After completion, create `.planning/phases/22-token-cleanup-chat-multi-campus/22-02-SUMMARY.md` following the summary template.
</output>
