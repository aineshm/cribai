---
phase: 22-token-cleanup-chat-multi-campus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/design-tokens.ts
autonomous: true
requirements:
  - DESIGN-05
must_haves:
  truths:
    - "design-tokens.ts is deleted — it no longer exists in the repository"
    - "No component or test imports from design-tokens.ts (zero import errors)"
    - "The app builds cleanly with zero TypeScript errors after deletion"
    - "globals.css remains the single authoritative source of design token values"
  artifacts:
    - path: "apps/web/lib/design-tokens.ts"
      provides: "MUST NOT exist after this plan"
  key_links:
    - from: "apps/web/app/globals.css"
      to: "Tailwind utilities"
      via: "@theme inline block"
      pattern: "@theme inline"
---

<objective>
Delete the orphaned `apps/web/lib/design-tokens.ts` file and verify the build remains clean.

Purpose: DESIGN-05 requires a single source of truth for design tokens. The CSS custom properties in `globals.css` (via the `@theme inline` block) are already the authoritative runtime source. The TypeScript file duplicates those values, has drifted (primary-700 is `#0D7377` in TS vs `#0f766e` in CSS), and has zero component imports — confirmed by grep. Deleting it eliminates the drift risk and satisfies DESIGN-05.

Output: Deleted file, passing build, grep confirming zero remaining references.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-token-cleanup-chat-multi-campus/22-RESEARCH.md
@.planning/phases/22-token-cleanup-chat-multi-campus/22-VALIDATION.md
</context>

<interfaces>
<!-- Key facts extracted from codebase inspection. No exploration needed. -->

From apps/web/lib/design-tokens.ts (the file to delete):
- Exports: colors, radii, shadows, spacing, typography
- Zero component imports confirmed (grep found only tsconfig.tsbuildinfo and PHASE_10_COMPLETE.md referencing the filename)
- Value divergence from globals.css: primary-700 is #0D7377 in TS, #0f766e in CSS

From apps/web/app/globals.css:
- Contains @theme inline block (lines 361-400) that maps CSS custom properties to Tailwind color utilities
- This is the authoritative runtime token source — deleting design-tokens.ts does not affect any rendered value
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete design-tokens.ts and verify zero importers</name>
  <files>apps/web/lib/design-tokens.ts</files>
  <action>
    Before deleting, run the verification grep to confirm zero real imports:
    `grep -r "design-tokens" apps/web --include="*.ts" --include="*.tsx"`

    Expected output: zero hits from component/page files. If any component imports are found, STOP and report — do not delete.

    If zero component imports confirmed: delete the file.
    `rm apps/web/lib/design-tokens.ts`

    Do NOT add any replacement file. The globals.css @theme inline block already satisfies DESIGN-05. No TypeScript bridge is needed.

    Rationale: The file header states it is "mirrored from globals.css" but it has drifted (primary-700 value differs). Keeping it creates dual-source drift. Deletion is the correct resolution.
  </action>
  <verify>
    <automated>grep -r "design-tokens" apps/web --include="*.ts" --include="*.tsx" && echo "IMPORTS_FOUND - do not delete" || echo "ZERO_IMPORTS - safe"</automated>
  </verify>
  <done>File `apps/web/lib/design-tokens.ts` does not exist. Grep for "design-tokens" in .ts/.tsx files returns zero results.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification — zero TypeScript errors after deletion</name>
  <files></files>
  <action>
    Run the Next.js build to confirm deletion causes no TypeScript import errors or missing module references:
    `pnpm --filter web build`

    The build must complete with zero errors. If build fails with a "Cannot find module 'design-tokens'" error, that means a component was importing it that the grep missed (e.g., via path alias). In that case: restore the file from git, identify the importer, and report.

    Expected: build passes because no component imports from this file.
  </action>
  <verify>
    <automated>pnpm --filter web build</automated>
  </verify>
  <done>Build exits with code 0. No "Cannot find module" errors referencing design-tokens. TypeScript compilation clean.</done>
</task>

</tasks>

<verification>
1. `grep -r "design-tokens" apps/web --include="*.ts" --include="*.tsx"` returns zero results
2. `pnpm --filter web build` exits with code 0
3. `ls apps/web/lib/design-tokens.ts` returns "No such file or directory"
</verification>

<success_criteria>
- design-tokens.ts is deleted from the repository
- Build passes with zero errors
- No component has a broken import
- globals.css @theme inline block remains the sole design token source (unchanged)
- DESIGN-05 satisfied: single source of truth, no duplicate values between TS and CSS
</success_criteria>

<output>
After completion, create `.planning/phases/22-token-cleanup-chat-multi-campus/22-01-SUMMARY.md` following the summary template.
</output>
