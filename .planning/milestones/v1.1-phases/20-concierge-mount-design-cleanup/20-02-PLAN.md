---
phase: 20-concierge-mount-design-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/notification-bell.tsx
  - apps/web/components/chat/conversation-sidebar.tsx
  - apps/web/components/share-button.tsx
  - apps/web/components/listing-photo-gallery.tsx
  - apps/web/components/listing-card.tsx
  - apps/web/components/cribai-chat.tsx
  - apps/web/components/listing-filters.tsx
  - apps/web/components/heart-button.tsx
  - apps/web/components/listing-location-map.tsx
  - apps/web/components/submit-listing-form.tsx
autonomous: true
requirements: [DESIGN-03]
gap_closure: true

must_haves:
  truths:
    - "No inline <svg> elements remain in v1.1 component files"
    - "All icons render correctly using Lucide imports"
    - "Heart button fill/unfill toggle still works with Lucide Heart icon"
  artifacts:
    - path: "apps/web/components/notification-bell.tsx"
      provides: "Bell icon via Lucide"
      contains: "import.*Bell.*from.*lucide-react"
    - path: "apps/web/components/chat/conversation-sidebar.tsx"
      provides: "MessageSquare icon via Lucide"
      contains: "import.*MessageSquare.*from.*lucide-react"
    - path: "apps/web/components/share-button.tsx"
      provides: "Check and Share2 icons via Lucide"
      contains: "import.*Check.*Share2.*from.*lucide-react"
    - path: "apps/web/components/listing-photo-gallery.tsx"
      provides: "X, ChevronLeft, ChevronRight icons via Lucide"
      contains: "import.*from.*lucide-react"
    - path: "apps/web/components/listing-card.tsx"
      provides: "ImageIcon via Lucide"
      contains: "import.*ImageIcon.*from.*lucide-react"
    - path: "apps/web/components/cribai-chat.tsx"
      provides: "Sparkles and Send icons via Lucide"
      contains: "import.*Sparkles.*Send.*from.*lucide-react"
    - path: "apps/web/components/listing-filters.tsx"
      provides: "X icon via Lucide"
      contains: "import.*X.*from.*lucide-react"
    - path: "apps/web/components/heart-button.tsx"
      provides: "Heart icon via Lucide with fill class toggle"
      contains: "import.*Heart.*from.*lucide-react"
    - path: "apps/web/components/listing-location-map.tsx"
      provides: "MapPin icon via Lucide"
      contains: "import.*MapPin.*from.*lucide-react"
    - path: "apps/web/components/submit-listing-form.tsx"
      provides: "Check icon via Lucide"
      contains: "import.*Check.*from.*lucide-react"
  key_links:
    - from: "all 10 component files"
      to: "lucide-react"
      via: "named imports replacing inline SVGs"
      pattern: "import.*from.*lucide-react"
---

<objective>
Replace all 14 remaining inline SVG elements across 10 component files with named Lucide icon imports, completing the DESIGN-03 requirement for consistent icon usage.

Purpose: Closes DESIGN-03 gap — Phase 10 established Lucide as the icon system but 14 inline Heroicon SVGs remained in legacy components.
Output: All 10 files updated with Lucide imports, zero inline `<svg>` elements in component directory.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-concierge-mount-design-cleanup/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline SVGs in simple files (6 files, 7 SVGs)</name>
  <files>
    apps/web/components/notification-bell.tsx,
    apps/web/components/chat/conversation-sidebar.tsx,
    apps/web/components/listing-card.tsx,
    apps/web/components/listing-filters.tsx,
    apps/web/components/listing-location-map.tsx,
    apps/web/components/submit-listing-form.tsx
  </files>
  <action>
    For each file, read the file first, then replace the inline SVG with the corresponding Lucide import. Preserve all existing className values and sizing. Do NOT mutate other logic.

    **Exact mapping (from research):**

    1. `notification-bell.tsx` (1 SVG):
       - Add `import { Bell } from 'lucide-react';`
       - Replace `<svg className="h-5 w-5 text-[var(--surface-500)]" ...>...</svg>` with `<Bell className="h-5 w-5 text-[var(--surface-500)]" strokeWidth={2} />`

    2. `chat/conversation-sidebar.tsx` (1 SVG):
       - Add `import { MessageSquare } from 'lucide-react';`
       - Replace `<svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5" ...>...</svg>` with `<MessageSquare className="h-5 w-5" strokeWidth={2} />`

    3. `listing-card.tsx` (1 SVG):
       - Add `import { ImageIcon } from 'lucide-react';`
       - Replace `<svg className="mx-auto h-8 w-8 text-[var(--surface-300)]" ...>...</svg>` with `<ImageIcon className="mx-auto h-8 w-8 text-[var(--surface-300)]" strokeWidth={1.5} />`

    4. `listing-filters.tsx` (1 SVG):
       - Add `import { X } from 'lucide-react';`
       - Replace `<svg className="h-3.5 w-3.5" ...>...</svg>` with `<X className="h-3.5 w-3.5" strokeWidth={2} />`

    5. `listing-location-map.tsx` (1 SVG):
       - Add `import { MapPin } from 'lucide-react';`
       - Replace the filled SVG pin with `<MapPin className="h-4 w-4 text-white" fill="currentColor" strokeWidth={0} />`
       - Note: The original SVG uses `fillRule="evenodd"` with a filled shape. Use `fill="currentColor"` and `strokeWidth={0}` to get a filled pin appearance. Lucide forwards standard SVG attributes as props.

    6. `submit-listing-form.tsx` (1 SVG):
       - Add `import { Check } from 'lucide-react';`
       - Replace `<svg className="h-8 w-8 text-[var(--primary-600)]" ...>...</svg>` with `<Check className="h-8 w-8 text-[var(--primary-600)]" strokeWidth={2} />`

    After each file edit, verify no `<svg` remains in that file with a mental check. After all 6 files, run the grep verification.
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && ! grep -l "<svg" apps/web/components/notification-bell.tsx apps/web/components/chat/conversation-sidebar.tsx apps/web/components/listing-card.tsx apps/web/components/listing-filters.tsx apps/web/components/listing-location-map.tsx apps/web/components/submit-listing-form.tsx 2>/dev/null; echo "EXIT:$?"</automated>
  </verify>
  <done>All 6 files show 0 `<svg>` occurrences. Lucide imports present in each file.</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline SVGs in complex files (4 files, 7 SVGs) and verify zero remaining</name>
  <files>
    apps/web/components/share-button.tsx,
    apps/web/components/listing-photo-gallery.tsx,
    apps/web/components/cribai-chat.tsx,
    apps/web/components/heart-button.tsx
  </files>
  <action>
    These files have multiple SVGs or special handling (heart fill state). Read each file first, then replace.

    **Exact mapping:**

    1. `share-button.tsx` (2 SVGs):
       - Add `import { Check, Share2 } from 'lucide-react';`
       - Replace check SVG (`<svg className="h-4 w-4 text-emerald-600" ...>...</svg>`) with `<Check className="h-4 w-4 text-emerald-600" strokeWidth={2} />`
       - Replace share SVG (`<svg className="h-4 w-4" ...>...</svg>`) with `<Share2 className="h-4 w-4" strokeWidth={2} />`

    2. `listing-photo-gallery.tsx` (3 SVGs):
       - Add `import { X, ChevronLeft, ChevronRight } from 'lucide-react';`
       - Replace X close SVG (`className="h-6 w-6"`) with `<X className="h-6 w-6" strokeWidth={2} />`
       - Replace left arrow SVG (`className="h-5 w-5"`, path `M15 19l-7-7 7-7`) with `<ChevronLeft className="h-5 w-5" strokeWidth={2} />`
       - Replace right arrow SVG (`className="h-5 w-5"`, path `M9 5l7 7-7 7`) with `<ChevronRight className="h-5 w-5" strokeWidth={2} />`

    3. `cribai-chat.tsx` (2 SVGs):
       - Add `import { Sparkles, Send } from 'lucide-react';`
       - Replace sparkle SVG (`className="h-7 w-7 text-[var(--primary-600)]"`) with `<Sparkles className="h-7 w-7 text-[var(--primary-600)]" strokeWidth={1.5} />`
       - Replace send arrow SVG (`className="h-4 w-4"`) with `<Send className="h-4 w-4" strokeWidth={2} />`

    4. `heart-button.tsx` (1 SVG — SPECIAL HANDLING):
       - Add `import { Heart } from 'lucide-react';`
       - Add `import { cn } from '@/lib/utils';` if not already imported
       - The existing SVG uses dynamic `fill` and `stroke` attributes based on `saved` state. Lucide Heart does not accept `fill` as a prop directly.
       - Replace the entire `<svg>...</svg>` block with:
         ```tsx
         <Heart
           className={cn(
             iconSize,
             'transition-transform duration-200',
             animating && 'animate-heart-pop',
             saved ? 'fill-red-500 stroke-red-500' : variant === 'overlay' ? 'stroke-white' : 'stroke-current'
           )}
           strokeWidth={2}
         />
         ```
       - This uses Tailwind's `fill-red-500` utility to set the SVG fill via CSS, preserving the saved/unsaved toggle behavior. Remove any `fill={...}` or `stroke={...}` JSX attributes that were on the old SVG — handle all styling through className.
       - Preserve the `animate-heart-pop` animation class and the `animating` state logic.
       - Read the existing file carefully to understand the full conditional logic for variant, saved, and animating states before making changes.

    After all 4 files are edited, run the full directory grep to confirm zero inline SVGs remain in all component files (excluding test files).
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && grep -rn "<svg" apps/web/components/ --include="*.tsx" | grep -v "__tests__" | grep -v ".test." | grep -v "node_modules"; echo "EXIT:$?"</automated>
  </verify>
  <done>
    - All 4 complex files updated with Lucide imports
    - Heart button fill toggle works via Tailwind classes (fill-red-500/stroke-red-500 when saved, fill-none otherwise)
    - Full grep returns empty output (zero inline SVGs in component files)
    - `pnpm --filter web test` passes (no regressions)
  </done>
</task>

</tasks>

<verification>
- `grep -rn "<svg" apps/web/components/ --include="*.tsx" | grep -v "__tests__" | grep -v ".test."` returns empty
- `pnpm --filter web test` full suite green
- `pnpm --filter web build` builds without errors
</verification>

<success_criteria>
- Zero inline `<svg>` elements in v1.1 component files (verified by grep)
- All 10 files import from `lucide-react` instead
- Heart button fill/unfill toggle preserved via Tailwind classes
- Build and test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-concierge-mount-design-cleanup/20-02-SUMMARY.md`
</output>
