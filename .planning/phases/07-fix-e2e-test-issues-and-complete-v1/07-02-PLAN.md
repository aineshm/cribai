---
phase: 07-fix-e2e-test-issues-and-complete-v1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/favicon.ico
  - apps/web/app/(campus)/[campusSlug]/layout.tsx
  - apps/web/components/mobile-nav.tsx
  - apps/web/components/submit-listing-form.tsx
  - apps/web/app/(campus)/[campusSlug]/notifications/page.tsx
  - apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx
  - apps/web/app/settings/profile/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Favicon loads on every page without a 404 in the network tab"
    - "Submit listing navigation and button say 'Share a Listing' instead of 'Submit Listing'"
    - "Notifications page does not auto-mark all notifications as read on load — user must click a button"
    - "Dashboard does not show a 'Recently Viewed' placeholder card"
    - "Profile settings page shows university name derived from campus context (read-only)"
  artifacts:
    - path: "apps/web/app/favicon.ico"
      provides: "Favicon file served automatically by Next.js App Router"
    - path: "apps/web/app/(campus)/[campusSlug]/notifications/page.tsx"
      provides: "Mark all as read button instead of auto-mark"
      contains: "Mark all as read"
    - path: "apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx"
      provides: "Dashboard without Recently Viewed placeholder"
  key_links:
    - from: "apps/web/app/favicon.ico"
      to: "browser"
      via: "Next.js App Router automatic serving"
      pattern: "favicon"
---

<objective>
Address remaining UX polish items from the E2E audit: add favicon, update submit listing copy, fix notification auto-read behavior, remove Recently Viewed placeholder, and show university on profile.

Purpose: These UX issues make the app feel unpolished — missing favicon causes 404s, placeholder cards break trust, auto-mark-as-read removes user control.
Output: Polished UX across navigation, notifications, dashboard, and profile.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fix-e2e-test-issues-and-complete-v1/07-RESEARCH.md

<interfaces>
From apps/web/app/(campus)/[campusSlug]/layout.tsx (line 165):
```
Submit Listing  <!-- nav link text to change -->
```

From apps/web/components/mobile-nav.tsx (line 132):
```
Submit Listing  <!-- mobile nav link text to change -->
```

From apps/web/components/submit-listing-form.tsx (line 336):
```
{isSubmitting ? 'Submitting...' : 'Submit Listing'}
```

From apps/web/app/(campus)/[campusSlug]/notifications/page.tsx (lines 98-103):
```typescript
// Currently marks all unread as read immediately on page load (server-side)
await supabase
  .from('notifications')
  .update({ is_read: true })
  .eq('user_id', userId)
  .eq('is_read', false);
```

From apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx (lines 144-152):
```tsx
{/* Recently Viewed */}
<div>Recently Viewed</div>
<!-- placeholder card content -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add favicon and update submit listing copy</name>
  <files>apps/web/app/favicon.ico, apps/web/app/(campus)/[campusSlug]/layout.tsx, apps/web/components/mobile-nav.tsx, apps/web/components/submit-listing-form.tsx</files>
  <action>
**Favicon:**
Generate a simple favicon.ico file for CampusNest. Use a minimal SVG-based approach: create `apps/web/app/icon.svg` with a simple house/home icon in emerald green (#10b981, matching the app's primary color). Next.js 15 App Router automatically serves `app/icon.svg` as the favicon via the metadata API. Alternatively, create `apps/web/app/favicon.ico` as a 32x32 .ico file. The simplest approach: create `apps/web/app/icon.tsx` using Next.js ImageResponse API to generate a dynamic favicon with the letter "C" in emerald on a white background (no external files needed).

Preferred approach (zero dependencies): Create `apps/web/app/icon.tsx`:
```tsx
import { ImageResponse } from 'next/og';

export const size = { width: 32, height: 32 };
export const contentType = 'image/png';

export default function Icon() {
  return new ImageResponse(
    (
      <div style={{
        fontSize: 24,
        background: '#10b981',
        width: '100%',
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        color: 'white',
        borderRadius: 6,
        fontWeight: 700,
      }}>
        C
      </div>
    ),
    { ...size }
  );
}
```

**Submit listing copy:**
1. In `apps/web/app/(campus)/[campusSlug]/layout.tsx` line 165: change "Submit Listing" to "Share a Listing"
2. In `apps/web/components/mobile-nav.tsx` line 132: change "Submit Listing" to "Share a Listing"
3. In `apps/web/components/submit-listing-form.tsx` line 336: change `'Submit Listing'` to `'Share Listing'` and `'Submitting...'` to `'Sharing...'`
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && test -f apps/web/app/icon.tsx && grep -c "Share" apps/web/app/\(campus\)/\[campusSlug\]/layout.tsx apps/web/components/mobile-nav.tsx apps/web/components/submit-listing-form.tsx</automated>
  </verify>
  <done>Favicon file exists (icon.tsx). All three files show "Share a Listing" or "Share Listing" instead of "Submit Listing".</done>
</task>

<task type="auto">
  <name>Task 2: Fix notification read behavior, remove Recently Viewed, add university to profile</name>
  <files>apps/web/app/(campus)/[campusSlug]/notifications/page.tsx, apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx, apps/web/app/settings/profile/page.tsx</files>
  <action>
**Notifications page — replace auto-mark-as-read with button:**
1. Remove the server-side auto-mark-as-read code (lines 98-103 that update `is_read` on page load).
2. Add a client component `MarkAllReadButton` (can be inline in the same file or a small separate component) that:
   - Shows a "Mark all as read" button when there are unread notifications
   - On click, calls a server action or fetch to `POST /api/notifications/mark-read` (or directly uses Supabase client) to update `is_read = true` for the user's unread notifications
   - After marking, refreshes the page data (use `router.refresh()` or revalidatePath)
3. The page should still fetch notifications but NOT mutate them on load. Keep the existing notification list rendering and unread styling (the blue dot for `!n.is_read`).

Note: Since the current page is a Server Component that does both the fetch and the mutation, the simplest fix is:
- Remove the `.update({ is_read: true })` block
- Add a small client component with the "Mark all as read" button that makes a fetch call
- Create a simple API route `apps/web/app/api/notifications/mark-read/route.ts` if one doesn't exist

**Dashboard — remove Recently Viewed placeholder:**
In `apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx`, remove the entire "Recently Viewed" section (lines ~144-152 and any associated JSX). Do NOT replace with anything — just remove the placeholder card entirely. Per user decision: deferred to v2.

**Profile — show university:**
In `apps/web/app/settings/profile/page.tsx`:
1. The page likely already fetches the user's profile. Add a query to get the campus name from `campus_configs` joined via the user's `campus_id` in profiles (or hardcode "University of Wisconsin-Madison" since that's the only campus in v1 — per PLAT-01).
2. Display the university name as a read-only field in the profile form, styled as a disabled input or plain text with a label "University".
3. If campus_id is not on the profile, derive from the URL campus context or display the v1 default.
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && pnpm --filter @campusnest/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Notifications page no longer auto-marks as read on load — has a "Mark all as read" button instead. Dashboard has no Recently Viewed section. Profile page shows university name as read-only field. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @campusnest/web exec tsc --noEmit` — TypeScript compiles clean
2. `pnpm --filter @campusnest/web test -- --run` — existing tests pass
3. `test -f apps/web/app/icon.tsx` — favicon file exists
4. `grep -c "Submit Listing" apps/web/app/\(campus\)/\[campusSlug\]/layout.tsx apps/web/components/mobile-nav.tsx` — returns 0 for both
5. `grep -c "is_read.*true" apps/web/app/\(campus\)/\[campusSlug\]/notifications/page.tsx` — no auto-mark on load
6. `grep -c "Recently Viewed" apps/web/app/\(campus\)/\[campusSlug\]/dashboard/page.tsx` — returns 0
</verification>

<success_criteria>
- Favicon served without 404 on page loads
- "Submit Listing" replaced with "Share a Listing" in desktop nav, mobile nav, and form button
- Notifications page renders with unread styling intact but does NOT auto-mark as read
- "Mark all as read" button available for explicit user action
- Dashboard has no Recently Viewed placeholder card
- Profile settings shows university name (read-only)
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-e2e-test-issues-and-complete-v1/07-02-SUMMARY.md`
</output>
