---
phase: 07-fix-e2e-test-issues-and-complete-v1
plan: 03
type: execute
wave: 1
depends_on: ["07-02"]
files_modified:
  - apps/web/app/icon.tsx
  - apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx
  - apps/web/app/api/notifications/mark-read/route.ts
  - apps/web/app/(campus)/[campusSlug]/layout.tsx
  - apps/web/components/mobile-nav.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "Favicon shows green 'CN' logo in browser tab, not just 'C'"
    - "Dashboard 2-card grid fills available space without awkward empty right column"
    - "Mark all as read button successfully marks dev user notifications as read"
    - "Red notification badge appears on notifications bell, not on Saved nav link"
  artifacts:
    - path: "apps/web/app/icon.tsx"
      provides: "Favicon with 'CN' text"
      contains: "CN"
    - path: "apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx"
      provides: "2-column grid layout for dashboard cards"
      contains: "lg:grid-cols-2"
    - path: "apps/web/app/api/notifications/mark-read/route.ts"
      provides: "Correct dev user ID resolution for mark-read"
      contains: "dev_user_id"
    - path: "apps/web/app/(campus)/[campusSlug]/layout.tsx"
      provides: "Saved nav link without false notification badge"
    - path: "apps/web/components/mobile-nav.tsx"
      provides: "Mobile Saved link without notification badge, notifications link keeps badge"
  key_links:
    - from: "apps/web/app/api/notifications/mark-read/route.ts"
      to: "apps/web/lib/dev-auth.ts"
      via: "dev_user_id cookie read or getDevUserById"
      pattern: "dev_user_id|getDevUserById|DEV_USER_COOKIE"
---

<objective>
Fix 4 UAT issues: favicon text, dashboard grid, notification mark-read with correct dev user ID, and notification badge placement on correct nav element.

Purpose: Address UAT test failures 5, 7, and 8 so the app behaves correctly in dev mode and has polished UI.
Output: Corrected favicon, dashboard layout, working mark-read, and proper badge placement.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-fix-e2e-test-issues-and-complete-v1/07-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/web/lib/dev-auth.ts:
```typescript
export const DEV_USER_COOKIE = 'dev_user_id';
export const DEFAULT_DEV_USER = DEV_USERS[0]!; // id: 'a0000000-0000-4000-8000-000000000001'
export function isDevAuthEnabled(): boolean;
export function getDevUserById(id: string): DevUser | undefined;
```

From apps/web/app/api/notifications/mark-read/route.ts (current — needs fix):
```typescript
// Line 24: BROKEN — hardcodes 'dev-user-1' instead of reading actual dev user UUID
const userId = user?.id ?? (isDevAuth ? 'dev-user-1' : null);
```

From apps/web/app/(campus)/[campusSlug]/layout.tsx (current — needs fix):
```typescript
// Lines 181-185: BROKEN — priceChangedSavesCount badge is on the Saved link
// This should NOT be here. The Saved link should have no badge.
// The badge concept for price-changed saves should be removed from Saved.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix favicon text and dashboard grid</name>
  <files>apps/web/app/icon.tsx, apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx</files>
  <action>
  In apps/web/app/icon.tsx:
  - Change the text content from 'C' to 'CN' (line 23)
  - Reduce fontSize from 24 to 16 so both letters fit in the 32x32 icon without clipping
  - Keep all other styles (green background, white text, border radius, font weight)

  In apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx:
  - Change `lg:grid-cols-3` to `lg:grid-cols-2` on line 51
  - Keep md:grid-cols-2 and grid-cols-1 as-is
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && grep -q 'CN' apps/web/app/icon.tsx && grep -q 'lg:grid-cols-2' apps/web/app/\(campus\)/\[campusSlug\]/dashboard/page.tsx && echo "PASS"</automated>
  </verify>
  <done>Favicon renders "CN" with appropriate font size. Dashboard uses 2-column grid on large screens.</done>
</task>

<task type="auto">
  <name>Task 2: Fix mark-read dev user ID and move notification badge from Saved to bell</name>
  <files>apps/web/app/api/notifications/mark-read/route.ts, apps/web/app/(campus)/[campusSlug]/layout.tsx, apps/web/components/mobile-nav.tsx</files>
  <action>
  In apps/web/app/api/notifications/mark-read/route.ts:
  - Import `cookies` from 'next/headers' (already imported), `DEV_USER_COOKIE`, `DEFAULT_DEV_USER` from dev-auth
  - Replace the hardcoded `'dev-user-1'` fallback on line 24 with proper dev user resolution:
    Read the `dev_user_id` cookie from the cookie store, fall back to `DEFAULT_DEV_USER.id` if no cookie set
  - The corrected line should be:
    ```
    const devUserId = isDevAuth ? (cookieStore.get(DEV_USER_COOKIE)?.value ?? DEFAULT_DEV_USER.id) : null;
    const userId = user?.id ?? devUserId;
    ```
  - Import path for dev-auth: `../../../../lib/dev-auth` (same level as the cribai route)

  In apps/web/app/(campus)/[campusSlug]/layout.tsx:
  - Remove the `priceChangedSavesCount` badge from the Saved nav link (lines 181-185). The Saved link should be a plain link with no badge indicator.
  - Keep the `priceChangedSavesCount` variable and its DB query — it may be useful for NotificationBell or future use. Just remove it from the Saved `<Link>` JSX.
  - The Saved link should become a simple link like Dashboard (no `relative` class needed, no `<span>` badge).

  In apps/web/components/mobile-nav.tsx:
  - Remove the `priceChangedSavesCount` badge from the mobile Saved link (lines 158-162). The Saved link should show "Saved" text only with no badge.
  - Keep the notifications badge on the Notifications link (lines 174-178) — that is correct.
  - Remove `priceChangedSavesCount` from the MobileNavProps interface and the destructured props since it's no longer used in this component.
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && grep -q 'DEV_USER_COOKIE\|DEFAULT_DEV_USER' apps/web/app/api/notifications/mark-read/route.ts && ! grep -q 'dev-user-1' apps/web/app/api/notifications/mark-read/route.ts && ! grep -q 'priceChangedSavesCount' apps/web/components/mobile-nav.tsx && echo "PASS"</automated>
  </verify>
  <done>Mark-read API resolves correct dev user UUID from cookie. Saved nav link (desktop and mobile) shows no notification badge. Notifications link in mobile nav retains its unread count badge.</done>
</task>

</tasks>

<verification>
- `grep 'CN' apps/web/app/icon.tsx` confirms favicon text
- `grep 'lg:grid-cols-2' apps/web/app/(campus)/[campusSlug]/dashboard/page.tsx` confirms grid fix
- `grep -v 'dev-user-1' apps/web/app/api/notifications/mark-read/route.ts` confirms no hardcoded ID
- `grep 'DEV_USER_COOKIE' apps/web/app/api/notifications/mark-read/route.ts` confirms proper resolution
- `pnpm turbo build --filter=web` passes
</verification>

<success_criteria>
- Favicon icon.tsx contains "CN" text with font size that fits 32x32
- Dashboard grid uses lg:grid-cols-2 (not lg:grid-cols-3)
- Mark-read API route uses actual dev user UUID from cookie, not hardcoded string
- Saved nav link in desktop layout has no red badge/indicator
- Saved nav link in mobile nav has no badge
- Notifications link in mobile nav retains its unread count badge
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-e2e-test-issues-and-complete-v1/07-03-SUMMARY.md`
</output>
