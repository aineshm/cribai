---
phase: 24-listing-ai-summary-verification-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/mock-listing-detail.ts
  - apps/web/components/listing/LeaseSummary.tsx
  - apps/web/components/listing/ListingContent.tsx
  - apps/web/components/listing/__tests__/LeaseSummary.test.tsx
autonomous: true
requirements:
  - DETAIL-03

must_haves:
  truths:
    - "LeaseSummary renders an AI prose paragraph above the structured lease grid when aiSummary is present"
    - "LeaseSummary renders correctly without aiSummary (field is optional — no broken existing consumers)"
    - "ListingContent passes listing.aiSummary to LeaseSummary"
    - "MOCK_LISTING_DETAIL includes a sample aiSummary string"
    - "Unit test asserts the prose block renders when aiSummary prop is supplied"
  artifacts:
    - path: "apps/web/lib/mock-listing-detail.ts"
      provides: "DetailedListing type with optional aiSummary field + mock value"
      contains: "aiSummary?"
    - path: "apps/web/components/listing/LeaseSummary.tsx"
      provides: "LeaseSummaryProps with aiSummary? + conditional prose render"
      contains: "aiSummary"
    - path: "apps/web/components/listing/ListingContent.tsx"
      provides: "Passes aiSummary={listing.aiSummary} to LeaseSummary"
      contains: "aiSummary={listing.aiSummary}"
    - path: "apps/web/components/listing/__tests__/LeaseSummary.test.tsx"
      provides: "Unit test covering aiSummary prose render"
      contains: "aiSummary"
  key_links:
    - from: "apps/web/components/listing/ListingContent.tsx"
      to: "apps/web/components/listing/LeaseSummary.tsx"
      via: "aiSummary prop"
      pattern: "aiSummary=\\{listing\\.aiSummary\\}"
    - from: "apps/web/components/listing/LeaseSummary.tsx"
      to: "aiSummary conditional render"
      via: "aiSummary && prose block"
      pattern: "\\{aiSummary"
---

<objective>
Add the AI-generated lease summary prose field to the listing detail page, closing DETAIL-03.

Purpose: DETAIL-03 requires a listing detail page that shows "landlord info card, amenities grid, and AI lease summary section." The landlord card and amenities grid already exist. The AI lease summary section exists as a component (LeaseSummary.tsx) but only renders structured lease data — it has no aiSummary prose field on the DetailedListing type or in the component props. This plan adds that field, wires it through, adds a sample mock value, and updates the test.

Output: DetailedListing.aiSummary optional field, LeaseSummary.tsx renders prose when present, ListingContent.tsx passes the prop, unit test green.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-listing-ai-summary-verification-sweep/24-RESEARCH.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From apps/web/lib/mock-listing-detail.ts (existing shape — add aiSummary? to this interface):
```typescript
export interface DetailedListing {
  readonly id: string;
  readonly title: string;
  // ... (other existing fields) ...
  readonly leaseSummary: LeaseSummary;
  // ADD: readonly aiSummary?: string;
}
```

From apps/web/components/listing/LeaseSummary.tsx (existing props — add aiSummary?):
```typescript
interface LeaseSummaryProps {
  readonly leaseSummary: LeaseSummaryType;
  // ADD: readonly aiSummary?: string;
}
```

From apps/web/components/listing/ListingContent.tsx (existing usage — add aiSummary prop):
```tsx
<LeaseSummary leaseSummary={listing.leaseSummary} />
// BECOMES:
<LeaseSummary leaseSummary={listing.leaseSummary} aiSummary={listing.aiSummary} />
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aiSummary field to DetailedListing type and mock data</name>
  <files>apps/web/lib/mock-listing-detail.ts</files>
  <behavior>
    - Test 1: DetailedListing type accepts aiSummary as an optional string field (TypeScript compile check — no runtime test needed)
    - Test 2: MOCK_LISTING_DETAIL.aiSummary is a non-empty string (used in Task 2 test)
  </behavior>
  <action>
    Read apps/web/lib/mock-listing-detail.ts in full.

    Add `readonly aiSummary?: string;` to the `DetailedListing` interface — place it after the `leaseSummary` field as the last field in the interface. Use the exact comment: `// AI-generated plain-language lease summary`.

    Add a sample aiSummary string to the `MOCK_LISTING_DETAIL` object. Use this value:
    `"This is a standard 12-month lease with utilities split between tenants. The no-subletting clause requires landlord approval. Security deposit is one month's rent, refundable within 21 days of move-out. Pet policy allows cats and small dogs under 25 lbs with a $300 non-refundable pet deposit."`

    The field is optional (`?:`). Do NOT make it required — existing mock data in other test fixtures must not break. Preserve all existing fields exactly.
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent/apps/web && pnpm exec vitest run components/listing 2>&1 | tail -20</automated>
  </verify>
  <done>DetailedListing interface has aiSummary?: string field. MOCK_LISTING_DETAIL has a sample aiSummary string. TypeScript build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render aiSummary prose in LeaseSummary.tsx and pass prop from ListingContent.tsx</name>
  <files>apps/web/components/listing/LeaseSummary.tsx, apps/web/components/listing/ListingContent.tsx, apps/web/components/listing/__tests__/LeaseSummary.test.tsx</files>
  <behavior>
    - Test 1: When aiSummary prop is provided, LeaseSummary renders a `<p>` element containing that text
    - Test 2: When aiSummary prop is omitted, LeaseSummary renders without error (no prose paragraph, no crash)
    - Test 3: The prose paragraph has accessible text visible to screen readers (not hidden)
  </behavior>
  <action>
    Read LeaseSummary.tsx, LeaseSummary.test.tsx (if it exists), and ListingContent.tsx in full before making changes.

    **LeaseSummary.tsx changes:**
    - Add `readonly aiSummary?: string;` to `LeaseSummaryProps` (or equivalent interface)
    - Destructure `aiSummary` in the function signature
    - Inside `CardContent`, render the prose block ABOVE the existing structured grid:
      ```tsx
      {aiSummary && (
        <p className="text-sm text-muted-foreground leading-relaxed">
          {aiSummary}
        </p>
      )}
      ```
    - Do NOT add an additional `motion.div` wrapper around the prose — it inherits the parent stagger. Keep the prose inside the existing `CardContent`.

    **ListingContent.tsx changes:**
    - Find the `<LeaseSummary leaseSummary={listing.leaseSummary} />` call
    - Update it to: `<LeaseSummary leaseSummary={listing.leaseSummary} aiSummary={listing.aiSummary} />`
    - No other changes to ListingContent.tsx

    **LeaseSummary.test.tsx changes:**
    - Read the existing test file first. If a test for "AI Lease Summary" section exists, add a new `it` case within that describe block: render with `aiSummary="Test summary text"` and assert `screen.getByText("Test summary text")` is in the document.
    - If no test file exists for LeaseSummary, create one at `apps/web/components/listing/__tests__/LeaseSummary.test.tsx` with:
      - One test: renders aiSummary prose when provided
      - One test: renders without error when aiSummary is omitted
    - Use the existing framer-motion mock (already configured in vitest.setup.ts) — no additional mock setup needed.
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent/apps/web && pnpm exec vitest run components/listing 2>&1 | tail -20</automated>
  </verify>
  <done>LeaseSummary.tsx accepts aiSummary prop and renders prose above structured grid when present. ListingContent.tsx passes listing.aiSummary. Unit tests pass (including the new aiSummary test cases). No TypeScript errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```
cd /Users/aineshmohan/Developer/ai-real-estate-agent/apps/web && pnpm exec vitest run components/listing && pnpm exec playwright test tests/e2e/listing-detail.spec.ts --project=chromium 2>&1 | tail -30
```

The listing-detail E2E spec should pass — it already covers the "AI Lease Summary" section. If the spec only checks for the heading and not the prose text, that is acceptable (the unit test covers prose rendering).
</verification>

<success_criteria>
1. `DetailedListing` interface in mock-listing-detail.ts has `readonly aiSummary?: string` field
2. `MOCK_LISTING_DETAIL` has a sample aiSummary string value
3. `LeaseSummary` component renders the prose paragraph above the structured grid when aiSummary is present
4. `LeaseSummary` renders without error when aiSummary is absent
5. `ListingContent` passes `aiSummary={listing.aiSummary}` to LeaseSummary
6. Unit tests pass — at minimum one test asserts prose renders when aiSummary is provided
7. `pnpm exec vitest run components/listing` exits green
</success_criteria>

<output>
After completion, create `.planning/phases/24-listing-ai-summary-verification-sweep/24-01-SUMMARY.md` with:
- Tasks completed
- Files modified with key changes
- Test results
- Any decisions made
</output>
