---
phase: 17-real-tool-integrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/014_api_cache_landlord_contacts.sql
  - packages/ai/src/tools/lib/api-cache.ts
  - packages/ai/src/tools/lib/google-places.ts
  - packages/ai/src/tools/lib/walkscore.ts
  - packages/ai/src/tools/__tests__/api-cache.test.ts
  - packages/ai/src/tools/__tests__/google-places.test.ts
  - packages/ai/src/tools/__tests__/walkscore.test.ts
  - packages/ai/src/tools/__tests__/helpers.ts
autonomous: true
requirements:
  - TOOLS-01
  - TOOLS-02
  - TOOLS-03

must_haves:
  truths:
    - "api_cache table exists with key (PK), response (JSONB), expires_at columns and RLS enabled"
    - "landlords table has phone and email text columns"
    - "listings table has landlord_id FK referencing landlords(id)"
    - "Google Places client can Text Search, get Place Details with reviews, and Nearby Search with category filtering"
    - "Walk Score client returns walk/transit/bike scores for an address+coords"
    - "Cache module reads non-expired entries and returns null for expired/missing entries"
    - "Cache module upserts entries with computed expires_at timestamp"
  artifacts:
    - path: "supabase/migrations/014_api_cache_landlord_contacts.sql"
      provides: "api_cache table, landlords phone/email columns, listings landlord_id FK"
      contains: "CREATE TABLE api_cache"
    - path: "packages/ai/src/tools/lib/api-cache.ts"
      provides: "getCached and setCache functions for Supabase-backed TTL cache"
      exports: ["getCached", "setCache"]
    - path: "packages/ai/src/tools/lib/google-places.ts"
      provides: "Google Places API (New) client functions"
      exports: ["textSearchPlace", "getPlaceDetails", "nearbySearch"]
    - path: "packages/ai/src/tools/lib/walkscore.ts"
      provides: "Walk Score API client"
      exports: ["getWalkScore"]
  key_links:
    - from: "packages/ai/src/tools/lib/api-cache.ts"
      to: "supabase api_cache table"
      via: "supabase.from('api_cache')"
      pattern: "from\\('api_cache'\\)"
    - from: "packages/ai/src/tools/lib/google-places.ts"
      to: "Google Places API (New)"
      via: "fetch to places.googleapis.com/v1"
      pattern: "places\\.googleapis\\.com"
    - from: "packages/ai/src/tools/lib/walkscore.ts"
      to: "Walk Score API"
      via: "fetch to api.walkscore.com"
      pattern: "api\\.walkscore\\.com"
---

<objective>
Create the foundation layer for Phase 17: the DB migration adding the api_cache table and landlord contact columns, plus three shared library modules (api-cache, google-places, walkscore) with full unit test coverage.

Purpose: The three tool handlers (Plan 02) depend on these shared modules for caching and external API access. Building and testing them first prevents integration surprises.
Output: Migration SQL, 3 lib modules, 3 test files, all tests passing.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-real-tool-integrations/17-CONTEXT.md
@.planning/phases/17-real-tool-integrations/17-RESEARCH.md

@packages/ai/src/tools/types.ts
@packages/ai/src/tools/__tests__/helpers.ts
@packages/ai/src/gemini-client.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/ai/src/tools/types.ts:
```typescript
export interface ToolContext {
  readonly supabase: SupabaseClient;
  readonly campusId: string;
  readonly campusSlug: string;
  readonly userId?: string;
}
```

From packages/ai/src/tools/__tests__/helpers.ts:
```typescript
export function createMockQueryBuilder(resolvedData, error): MockQueryBuilder;
export function createMockContext(overrides?): ToolContext;
```

Existing migration numbering: 013_missions_schema.sql is the latest.
Next migration: 014_api_cache_landlord_contacts.sql
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration + api-cache module with tests</name>
  <files>
    supabase/migrations/014_api_cache_landlord_contacts.sql,
    packages/ai/src/tools/lib/api-cache.ts,
    packages/ai/src/tools/__tests__/api-cache.test.ts,
    packages/ai/src/tools/__tests__/helpers.ts
  </files>
  <action>
    1. Create migration `014_api_cache_landlord_contacts.sql`:
       - CREATE TABLE api_cache (key text PRIMARY KEY, response jsonb NOT NULL, expires_at timestamptz NOT NULL, created_at timestamptz DEFAULT now())
       - CREATE INDEX idx_api_cache_expires ON api_cache (expires_at)
       - ALTER TABLE api_cache ENABLE ROW LEVEL SECURITY (no user-facing policies -- service role only)
       - ALTER TABLE landlords ADD COLUMN IF NOT EXISTS phone text
       - ALTER TABLE landlords ADD COLUMN IF NOT EXISTS email text
       - ALTER TABLE listings ADD COLUMN IF NOT EXISTS landlord_id uuid REFERENCES landlords(id)
       - CREATE INDEX idx_listings_landlord ON listings (landlord_id) WHERE landlord_id IS NOT NULL

    2. Create `packages/ai/src/tools/lib/api-cache.ts`:
       - `getCached<T>(supabase, key): Promise<T | null>` -- query api_cache by key, return null if missing or expired (expires_at < now)
       - `setCache(supabase, key, response, ttlMs): Promise<void>` -- upsert with computed expires_at = Date.now() + ttlMs
       - Use the research pattern (check expiry on read, upsert on write with onConflict: 'key')
       - All params readonly, immutable patterns

    3. Create `packages/ai/src/tools/__tests__/api-cache.test.ts`:
       - Test: getCached returns data when entry exists and not expired
       - Test: getCached returns null when entry is expired
       - Test: getCached returns null when no entry exists
       - Test: setCache calls upsert with correct key, response, and computed expires_at
       - Mock supabase.from('api_cache') using the existing createMockQueryBuilder pattern
       - Add `upsert` method to MockQueryBuilder in helpers.ts

    4. Update `helpers.ts` MockQueryBuilder to include `upsert: ReturnType<typeof vi.fn>` with proper chaining support.
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && pnpm --filter @campusnest/ai test -- --run src/tools/__tests__/api-cache.test.ts</automated>
  </verify>
  <done>Migration SQL file exists with api_cache table, landlord phone/email columns, and listings landlord_id FK. api-cache module exports getCached and setCache. All cache tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Google Places + Walk Score API client modules with tests</name>
  <files>
    packages/ai/src/tools/lib/google-places.ts,
    packages/ai/src/tools/lib/walkscore.ts,
    packages/ai/src/tools/__tests__/google-places.test.ts,
    packages/ai/src/tools/__tests__/walkscore.test.ts
  </files>
  <behavior>
    Google Places:
    - textSearchPlace(address, apiKey) returns place_id string or null when no results
    - textSearchPlace includes city/state in query for disambiguation
    - getPlaceDetails(placeId, apiKey, fieldMask) returns PlaceDetailsResult with reviews array
    - getPlaceDetails throws on non-OK response
    - nearbySearch(lat, lon, radius, types, apiKey) returns array of NearbyPlace objects
    - nearbySearch returns empty array when no results

    Walk Score:
    - getWalkScore(address, lat, lon, apiKey) returns WalkScoreResult with walkscore, transit, bike
    - getWalkScore returns null scores when API returns status != 1
    - getWalkScore handles network errors gracefully (returns null-score result, not throw)
  </behavior>
  <action>
    1. Create `packages/ai/src/tools/lib/google-places.ts`:
       - Types: PlaceReview (rating, text, authorAttribution, relativePublishTimeDescription, publishTime), PlaceDetailsResult (id, displayName, rating, userRatingCount, reviews), NearbyPlace (displayName, formattedAddress, types, location)
       - `textSearchPlace(address: string, apiKey: string): Promise<string | null>` -- POST to places.googleapis.com/v1/places:searchText with FieldMask 'places.id,places.displayName', return first place ID
       - `getPlaceDetails(placeId: string, apiKey: string, fieldMask: string): Promise<PlaceDetailsResult>` -- GET places/{placeId} with field mask header
       - `nearbySearch(lat: number, lon: number, radiusMeters: number, includedTypes: string[], apiKey: string): Promise<readonly NearbyPlace[]>` -- POST places:searchNearby with locationRestriction circle
       - All functions use native fetch. Headers: X-Goog-Api-Key, X-Goog-FieldMask, Content-Type
       - Throw on non-OK responses with status code in error message

    2. Create `packages/ai/src/tools/lib/walkscore.ts`:
       - Types: WalkScoreResult (walkscore, description, transit: {score, description} | null, bike: {score, description} | null)
       - `getWalkScore(address: string, lat: number, lon: number, apiKey: string): Promise<WalkScoreResult>` -- GET api.walkscore.com/score with format=json, transit=1, bike=1
       - Return {walkscore: null, ...} if response status != 1 (score unavailable)
       - Wrap fetch in try/catch -- return null-score result on network failure (graceful degradation per user decisions)

    3. Write tests FIRST (TDD red), then implement:
       - Mock global.fetch using vi.stubGlobal or vi.spyOn(globalThis, 'fetch')
       - Test all behaviors listed above
       - Verify correct URLs, headers, and request bodies are passed to fetch
       - Verify error cases (non-OK response, missing data, network failure)
  </action>
  <verify>
    <automated>cd /Users/aineshmohan/Developer/ai-real-estate-agent && pnpm --filter @campusnest/ai test -- --run src/tools/__tests__/google-places.test.ts src/tools/__tests__/walkscore.test.ts</automated>
  </verify>
  <done>google-places.ts exports textSearchPlace, getPlaceDetails, nearbySearch with correct types. walkscore.ts exports getWalkScore with graceful degradation. All tests pass with mocked fetch.</done>
</task>

</tasks>

<verification>
All 3 test files pass:
```bash
pnpm --filter @campusnest/ai test -- --run src/tools/__tests__/api-cache.test.ts src/tools/__tests__/google-places.test.ts src/tools/__tests__/walkscore.test.ts
```
Migration file exists at supabase/migrations/014_api_cache_landlord_contacts.sql with correct SQL.
Lib modules exist at packages/ai/src/tools/lib/ with proper exports.
</verification>

<success_criteria>
- Migration 014 creates api_cache table, adds landlords phone/email, adds listings landlord_id FK
- api-cache module: getCached returns null for expired/missing, returns data for valid entries; setCache upserts correctly
- google-places module: 3 functions for Text Search, Place Details, Nearby Search with proper types
- walkscore module: getWalkScore returns scores or null-score result on failure
- All unit tests pass with mocked external dependencies (no real API calls)
</success_criteria>

<output>
After completion, create `.planning/phases/17-real-tool-integrations/17-01-SUMMARY.md`
</output>
