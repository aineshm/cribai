# Deferred Items - Phase 01

## Pre-existing Issues

- `apps/web/tests/e2e/auth.spec.ts` has TypeScript syntax errors (not valid TS). Needs to be rewritten or excluded from tsconfig include. Discovered during 01-01 typecheck.
