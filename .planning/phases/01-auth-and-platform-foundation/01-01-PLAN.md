---
phase: 01-auth-and-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/callback/route.ts
  - apps/web/app/auth/callback/route.ts
  - apps/web/app/layout.tsx
  - apps/web/package.json
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "User clicking a magic link lands on the authenticated campus page without redirect errors"
    - "Non-.edu email shows inline validation error before magic link is sent"
    - "Expired or used magic link redirects to login with toast explaining the issue"
    - "User session persists across browser refresh and tab close/reopen"
  artifacts:
    - path: "apps/web/app/auth/callback/route.ts"
      provides: "Consolidated auth callback handling PKCE + token_hash flows"
      contains: "new URL(next, origin)"
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "Login page with .edu validation and error toast"
      contains: "endsWith('.edu')"
    - path: "apps/web/app/layout.tsx"
      provides: "Root layout with Toaster component"
      contains: "Toaster"
  key_links:
    - from: "apps/web/app/(auth)/login/page.tsx"
      to: "/auth/callback"
      via: "emailRedirectTo in signInWithOtp options"
      pattern: "emailRedirectTo.*auth/callback"
    - from: "apps/web/app/auth/callback/route.ts"
      to: "/login?error="
      via: "redirect on auth failure"
      pattern: "login\\?error="
    - from: "apps/web/app/(auth)/login/page.tsx"
      to: "sonner toast"
      via: "useEffect reading URL error param"
      pattern: "toast\\.error"
---

<objective>
Fix the broken magic link auth flow, add .edu email validation, and set up error notifications.

Purpose: Auth is currently broken (redirect bug in callback, duplicate routes, no .edu check). Nothing else can be tested or shipped until users can sign in reliably.
Output: Working magic link sign-in flow with .edu validation and error feedback via toast notifications.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-and-platform-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing code the executor needs to modify -->

From apps/web/app/(auth)/login/page.tsx (full file — modify in place):
- Client component with useState for email, loading, sent, error
- Calls `supabase.auth.signInWithOtp({ email, options: { emailRedirectTo } })`
- Currently points emailRedirectTo to `/callback` (WRONG — should be `/auth/callback`)
- No .edu validation exists

From apps/web/app/(auth)/callback/route.ts (BROKEN — to be DELETED):
- Uses string template `${origin}${next}` for redirect (unsafe)
- Duplicate of auth/callback/route.ts

From apps/web/app/auth/callback/route.ts (WORKING — to be enhanced):
- Uses `new URL(next, origin)` for redirect (correct pattern)
- Handles both PKCE code and token_hash flows
- Missing: error_code param handling for expired links

From apps/web/app/layout.tsx:
- Root layout with font setup, no Toaster component yet

From apps/web/middleware.ts:
- Already calls `getUser()` on every request (session refresh works — AUTH-02)
- Protects /*/cribai routes, remembers last_campus cookie
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auth callback, add .edu validation, install sonner</name>
  <files>
    apps/web/app/(auth)/callback/route.ts,
    apps/web/app/auth/callback/route.ts,
    apps/web/app/(auth)/login/page.tsx,
    apps/web/app/layout.tsx,
    apps/web/package.json
  </files>
  <action>
    1. Install sonner: `pnpm add sonner --filter @campusnest/web`

    2. DELETE `apps/web/app/(auth)/callback/route.ts` entirely. This is the broken route that uses unsafe string concatenation for redirects. The working version lives at `apps/web/app/auth/callback/route.ts`.

    3. Update `apps/web/app/auth/callback/route.ts`:
       - Add `error_code` param handling at the top of the GET handler. If `error_code` is present (Supabase sends this for expired/used links), redirect to `/login?error=link_expired`.
       - Change the default redirect fallback from `/` to `/uw-madison/cribai` when no `next` param and no `last_campus` cookie exists.
       - Keep the existing `new URL(next, origin)` pattern (correct).

    4. Update `apps/web/app/(auth)/login/page.tsx`:
       - Add `isEduEmail` function: extract domain from email, check `domain.endsWith('.edu')`. Handle edge cases (empty string, no @ sign).
       - In `handleSubmit`, before `signInWithOtp`, call `isEduEmail(email)`. If false, `setError('CampusNest requires a .edu email address')` and return.
       - Change `emailRedirectTo` from `${window.location.origin}/callback` to `${window.location.origin}/auth/callback` (points to the working route).
       - Add a `useEffect` that reads `error` search param from URL on mount. Map: `auth_failed` -> "Your magic link has expired or was already used. Please request a new one.", `link_expired` -> same message, `missing_code` -> "Invalid sign-in link. Please request a new one.", `config` -> "Sign-in is temporarily unavailable. Please try again later." Show via `toast.error()` from sonner. Clear the param from URL using `window.history.replaceState`.
       - Import `{ toast }` from `'sonner'`.

    5. Update `apps/web/app/layout.tsx`:
       - Add `import { Toaster } from 'sonner'` and place `<Toaster position="top-center" richColors />` inside the `<body>` tag, after `{children}`.

    Extract `isEduEmail` to a testable utility at `apps/web/lib/edu-validation.ts` so it can be unit tested. Export it. Import it in the login page.
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `(auth)/callback/route.ts` is deleted
    - `auth/callback/route.ts` handles error_code param, defaults to /uw-madison/cribai
    - Login page blocks non-.edu emails with inline error
    - Login page emailRedirectTo points to /auth/callback
    - Login page shows toast on URL error params
    - Sonner Toaster is in root layout
    - `isEduEmail` utility exists and is exported
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for edu validation and auth callback</name>
  <files>
    apps/web/lib/__tests__/edu-validation.test.ts,
    apps/web/vitest.config.ts
  </files>
  <action>
    1. Create `apps/web/vitest.config.ts` if it does not exist. Configure it for the web app:
       - Use `@vitejs/plugin-react` or just default vitest config
       - Set test include pattern to `lib/__tests__/**/*.test.ts`
       - Set environment to `jsdom` for any component tests later
       - Add path alias resolution matching the web app's tsconfig if needed

    2. Create `apps/web/lib/__tests__/edu-validation.test.ts`:
       - Import `isEduEmail` from `../edu-validation`
       - Test cases from Task 1 behavior block:
         - 'student@wisc.edu' -> true
         - 'student@university.edu' -> true
         - 'user@gmail.com' -> false
         - 'user@edu.com' -> false
         - '' -> false
         - 'no-at-sign' -> false
         - 'STUDENT@WISC.EDU' -> true (case insensitive)
         - 'student@sub.university.edu' -> true (subdomain)

    3. Ensure `vitest` is in devDependencies. If not: `pnpm add -D vitest --filter @campusnest/web`

    4. Add a `test` script to `apps/web/package.json` if missing: `"test": "vitest run"`
  </action>
  <verify>
    <automated>cd "/Users/aineshmohan/Library/Mobile Documents/com~apple~CloudDocs/certs_and_projects/ai-real-estate-agent" && pnpm --filter @campusnest/web test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All edu-validation tests pass
    - Vitest config exists for web app
    - Test script in package.json works
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter @campusnest/web exec tsc --noEmit` passes (no type errors)
- `pnpm --filter @campusnest/web test` passes (edu validation tests green)
- `apps/web/app/(auth)/callback/route.ts` does NOT exist (deleted)
- `apps/web/app/auth/callback/route.ts` contains `error_code` handling
- `apps/web/app/(auth)/login/page.tsx` contains `.edu` validation and `/auth/callback` redirect
</verification>

<success_criteria>
- Magic link callback uses safe URL construction and handles expired links gracefully
- Non-.edu emails are blocked before any server call with user-friendly message
- Toast notifications work for auth error feedback
- Session persistence (AUTH-02) is confirmed by existing middleware getUser() refresh pattern
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-and-platform-foundation/01-01-SUMMARY.md`
</output>
