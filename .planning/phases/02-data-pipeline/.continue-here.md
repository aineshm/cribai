---
phase: 02-data-pipeline
task: scraper-fix
total_tasks: 1
status: in_progress
last_updated: 2026-03-06T05:29:07.140Z
---

<current_state>
Fixing apartments.com scraper that returns 0 listings (403 blocked + wrong URL + outdated selectors + broken coordinate parsing). Three commits already pushed, one more uncommitted with selector/URL fixes still needs testing.
</current_state>

<completed_work>

- Commit b2986e0: Added stealth headers (Accept, Sec-Fetch-*), random delays (1.5-4s), reduced rate 20->12 req/min, block page detection
- Commit 9d6f137: Fixed CRITICAL WKB coordinate parsing (was 0,0 Gulf of Guinea), removed invalid campus slug from URL, added Cloudflare detection, bumped GH Actions timeout 30->45min
- Phase 4 context gathered and committed (04-CONTEXT.md) — separate work, done
</completed_work>

<remaining_work>

- UNCOMMITTED: Updated search page selectors to `.placard-content, article.placard, li.mortar-wrapper` (user confirmed `placard-content` is current)
- UNCOMMITTED: Added `CAMPUS_TO_CITY` mapping (`uw-madison` -> `madison-wi`) for correct apartments.com URL path
- UNCOMMITTED: Added JSON-LD extraction as primary data source on detail pages (stable across redesigns)
- UNCOMMITTED: Added broader CSS selector fallbacks for rent, beds/baths, sqft on detail pages
- UNCOMMITTED: Added viewport 1920x1080, `--disable-blink-features=AutomationControlled`
- UNCOMMITTED: Added diagnostic logging (page title, URL, body snippet) when no cards found
- UNCOMMITTED: Added optional `searchSlug` to `ScraperConfig` in base-scraper.ts
- All uncommitted changes typecheck clean, 27/27 tests pass
- NEED TO TEST: Run scraper locally or via GH Actions to verify listings are actually scraped
- NEED: `SUPABASE_SECRET_KEY` (service role key) in .env.local to test locally — currently empty
</remaining_work>

<decisions_made>

- Use city-state slug mapping (`CAMPUS_TO_CITY`) rather than geocoding or API lookup — simple, works for known campuses
- JSON-LD as primary extraction source on detail pages — more stable than CSS selectors across redesigns
- Multiple fallback selectors rather than single exact match — resilient to partial HTML changes
- Diagnostic body snippet logging on failure — helps debug remotely via GH Actions logs
</decisions_made>

<blockers>
- Cannot test locally: SUPABASE_SECRET_KEY is empty in .env.local. User can either paste it or test via GH Actions (secrets are configured there).
- apartments.com actively blocks automated requests (even WebFetch gets 403). Stealth may still not be enough — may need proxy rotation as next escalation.
</blockers>

<context>
The scraper had multiple layered bugs: wrong coordinates (WKB parsing), wrong URL path (campus slug vs city slug), outdated selectors (placard-content), and insufficient stealth. We fixed all of them incrementally. The uncommitted changes are the selector/URL fixes. Next step is to commit, push, and trigger the GH Actions workflow to test, OR get the service role key to test locally.
</context>

<next_action>
1. Commit and push the uncommitted selector/URL changes
2. Trigger GH Actions workflow_dispatch to test: `gh workflow run nightly-scrape.yml`
3. Check job logs for diagnostic output (card count, page title, body snippet)
4. If still blocked, escalate to proxy rotation
</next_action>
