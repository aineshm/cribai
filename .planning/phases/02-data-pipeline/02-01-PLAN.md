---
phase: 02-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_phase2_photos_history.sql
  - services/scraper/scrapers/base-scraper.ts
  - services/scraper/scrapers/apartments-com.ts
  - services/scraper/normalizer.ts
  - services/scraper/run.ts
  - services/scraper/package.json
  - packages/types/src/listing.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-06]

must_haves:
  truths:
    - "Scraper extracts up to 5 photo URLs per listing from Apartments.com detail pages"
    - "Listings with no rent are still scraped (address is required minimum)"
    - "Scraper outputs structured metrics JSON and exits non-zero when 0 listings scraped"
    - "Stale listings (30+ days inactive) are archived to listing_history then deleted"
    - "Photo URLs and source URL are stored in the listings table"
  artifacts:
    - path: "supabase/migrations/005_phase2_photos_history.sql"
      provides: "photo_urls column, nullable rent, source_url column, listing_history table"
      contains: "listing_history"
    - path: "services/scraper/scrapers/base-scraper.ts"
      provides: "Updated RawListing with photoUrls, sourceUrl, optional rent"
      exports: ["RawListing", "BaseScraper", "ScraperConfig"]
    - path: "services/scraper/scrapers/apartments-com.ts"
      provides: "Photo extraction, optional rent handling, stealth plugin"
      min_lines: 200
    - path: "services/scraper/run.ts"
      provides: "Metrics tracking, archive-then-delete, exit-on-zero"
      min_lines: 80
  key_links:
    - from: "services/scraper/scrapers/apartments-com.ts"
      to: "services/scraper/scrapers/base-scraper.ts"
      via: "RawListing interface with photoUrls field"
      pattern: "photoUrls"
    - from: "services/scraper/run.ts"
      to: "listing_history table"
      via: "archive INSERT + listings DELETE in sequence"
      pattern: "listing_history"
    - from: "services/scraper/run.ts"
      to: "stdout"
      via: "metrics JSON output for CI parsing"
      pattern: "::metrics::"
---

<objective>
Extend the existing Apartments.com scraper to extract photos, handle optional rent, track metrics, and manage listing lifecycle (archive + delete stale).

Purpose: Real listing data with photos is the foundation for the entire CampusNest experience. This plan makes the scraper production-ready with photo extraction, resilience (optional rent), lifecycle management (archive stale listings), and CI-friendly metrics output.

Output: Updated scraper that extracts photos, handles partial listings, archives stale data, and outputs structured metrics. Database migration for photo storage and listing history.
</objective>

<execution_context>
@/Users/aineshmohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aineshmohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@.planning/phases/02-data-pipeline/02-RESEARCH.md
@.planning/phases/02-data-pipeline/02-VALIDATION.md

@services/scraper/scrapers/base-scraper.ts
@services/scraper/scrapers/apartments-com.ts
@services/scraper/normalizer.ts
@services/scraper/run.ts
@packages/types/src/listing.ts
@supabase/migrations/001_initial_schema.sql

<interfaces>
<!-- Key types and contracts the executor needs -->

From services/scraper/scrapers/base-scraper.ts:
```typescript
export interface RawListing {
  readonly externalId: string;
  readonly source: string;
  readonly address: string;
  readonly rentMonthly: number;  // Will become number | null
  readonly bedrooms: number | null;
  readonly bathrooms: number | null;
  readonly sqft: number | null;
  readonly amenities: readonly string[];
  readonly availableDate: string | null;
  readonly latitude: number | null;
  readonly longitude: number | null;
  readonly rawData: Record<string, unknown>;
  // New fields to add: photoUrls, sourceUrl
}
```

From services/scraper/normalizer.ts:
```typescript
export interface NormalizedListing {
  // Same shape as RawListing — needs photoUrls, sourceUrl, nullable rentMonthly
}
export function normalizeListing(raw: RawListing): NormalizedListing;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration + type updates + stealth install</name>
  <files>
    supabase/migrations/005_phase2_photos_history.sql,
    services/scraper/scrapers/base-scraper.ts,
    services/scraper/normalizer.ts,
    packages/types/src/listing.ts,
    services/scraper/package.json,
    services/scraper/__tests__/normalizer.test.ts
  </files>
  <behavior>
    - Test: normalizeListing passes through photoUrls array unchanged
    - Test: normalizeListing passes through sourceUrl string unchanged
    - Test: normalizeListing handles null rentMonthly without error (no Math.round on null)
    - Test: normalizeListing with empty photoUrls returns empty array
  </behavior>
  <action>
    1. Create migration `005_phase2_photos_history.sql`:
       - `ALTER TABLE listings ADD COLUMN photo_urls text[] DEFAULT '{}'`
       - `ALTER TABLE listings ADD COLUMN source_url text`
       - `ALTER TABLE listings ALTER COLUMN rent_monthly DROP NOT NULL`
       - Create `listing_history` table: id (uuid PK), campus_id (FK campus_configs), external_id (text NOT NULL), source (text NOT NULL), address (text NOT NULL), rent_monthly (numeric nullable), first_seen_at (timestamptz), last_seen_at (timestamptz), archived_at (timestamptz DEFAULT now())
       - Add indexes: `idx_listing_history_campus` on (campus_id, archived_at DESC), `idx_listing_history_address` on (address)
       - Enable RLS on listing_history with select policy for authenticated users matching campus_id via profiles table

    2. Update `RawListing` in base-scraper.ts:
       - Add `readonly photoUrls: readonly string[]`
       - Add `readonly sourceUrl: string`
       - Change `rentMonthly: number` to `rentMonthly: number | null`

    3. Update `NormalizedListing` in normalizer.ts:
       - Add `readonly photoUrls: readonly string[]`
       - Add `readonly sourceUrl: string`
       - Change `rentMonthly: number` to `rentMonthly: number | null`
       - Update `normalizeListing`: pass through photoUrls and sourceUrl, guard Math.round with null check on rentMonthly

    4. Update `listingSchema` in packages/types/src/listing.ts:
       - Change `rentMonthly` from `z.number()` to `z.number().nullable()`
       - Add `photoUrls: z.array(z.string()).default([])`
       - Add `sourceUrl: z.string().nullable().default(null)`

    5. Install stealth packages: `cd services/scraper && pnpm add playwright-extra puppeteer-extra-plugin-stealth`

    6. Extend existing normalizer tests with photo and nullable rent cases.
  </action>
  <verify>
    <automated>pnpm --filter @campusnest/scraper test -- --run __tests__/normalizer.test.ts</automated>
  </verify>
  <done>Migration file exists with photo_urls, source_url, nullable rent, and listing_history table. RawListing and NormalizedListing include photoUrls/sourceUrl/nullable rent. Listing Zod schema updated. Stealth packages installed. Normalizer tests pass including new photo/rent cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Photo extraction + optional rent in scraper + metrics and archive in run.ts</name>
  <files>
    services/scraper/scrapers/apartments-com.ts,
    services/scraper/run.ts,
    services/scraper/__tests__/photo-extraction.test.ts,
    services/scraper/__tests__/metrics.test.ts,
    services/scraper/__tests__/staleness.test.ts
  </files>
  <behavior>
    - Test: extractPhotos returns up to 5 URLs from JSON-LD data
    - Test: extractPhotos falls back to OG meta tag when JSON-LD has no images
    - Test: extractPhotos falls back to carousel DOM selectors
    - Test: extractPhotos returns empty array when no photos found
    - Test: extractPhotos deduplicates URLs
    - Test: extractListing returns listing with null rent when no rent found (does NOT return null)
    - Test: extractListing still returns null when no address found
    - Test: outputMetrics writes correct JSON to stdout
    - Test: process exits with code 1 when upserted is 0
    - Test: archiveStaleListings inserts into listing_history and deletes from listings where inactive 30+ days
  </behavior>
  <action>
    1. Add `extractPhotos` method to ApartmentsComScraper (private async):
       - Strategy 1: Parse JSON-LD `script[type="application/ld+json"]` for `image` array or `photo[].contentUrl`
       - Strategy 2: OG meta tag `meta[property="og:image"]` for hero fallback
       - Strategy 3: Carousel DOM selectors `.carouselInner img, [data-tag_section="hero"] img, picture source`
       - Deduplicate, filter to http URLs only, cap at 5
       - See RESEARCH.md for exact implementation pattern

    2. Update `extractListing` in apartments-com.ts:
       - Call `extractPhotos(page, log)` to get photo URLs
       - Make rent optional: if parseRent returns null, set rentMonthly to null instead of returning null (address is the only required field per user decision)
       - Add `sourceUrl: url` to the returned RawListing
       - Add `photoUrls` to the returned RawListing

    3. Integrate stealth plugin in ApartmentsComScraper:
       - Import `chromium` from `playwright-extra` and `stealthPlugin` from `puppeteer-extra-plugin-stealth`
       - Call `chromium.use(stealthPlugin())` before crawler creation
       - Pass `launcher: chromium` in `launchContext` of PlaywrightCrawler config

    4. Update `run.ts` for metrics, archive, and exit-on-zero:
       - Define `ScrapeMetrics` interface: { upserted, staleMarked, archived, deleted, errors }
       - Track metrics throughout the scrape loop (count upserted per scraper, stale marked, etc.)
       - After stale marking: archive listings where `is_active = false AND last_seen_at < 30 days ago` — INSERT into listing_history (campus_id, external_id, source, address, rent_monthly, first_seen_at, last_seen_at), then DELETE from listings with same WHERE condition
       - Add photo_urls and source_url to upsert rows
       - Output metrics as `::metrics::${JSON.stringify(metrics)}` on last line
       - Exit with process.exit(1) if total upserted === 0

    5. Create test files:
       - `photo-extraction.test.ts`: Unit test extractPhotos with mocked Playwright Page (mock locator/textContent/getAttribute). Test JSON-LD path, OG fallback, carousel fallback, dedup, max 5 cap.
       - `metrics.test.ts`: Test metrics output format and exit-on-zero behavior. Mock console.log and process.exit.
       - `staleness.test.ts`: Test archive logic — mock Supabase client, verify correct queries for select inactive 30+ days, insert to listing_history, delete from listings.
  </action>
  <verify>
    <automated>pnpm --filter @campusnest/scraper test</automated>
  </verify>
  <done>Scraper extracts up to 5 photos per listing from JSON-LD/OG/carousel with dedup. Listings with no rent are saved (address-only minimum). Stealth plugin integrated. run.ts tracks and outputs metrics JSON, archives 30-day stale listings to listing_history, deletes archived listings, and exits non-zero on 0 upserted. All scraper tests pass.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @campusnest/scraper test` — all scraper tests pass (normalizer, photo-extraction, metrics, staleness)
- `pnpm typecheck` — no TypeScript errors across monorepo (nullable rent handled everywhere)
- Migration file exists at supabase/migrations/005_phase2_photos_history.sql
</verification>

<success_criteria>
- RawListing interface includes photoUrls (string[]), sourceUrl (string), nullable rentMonthly
- ApartmentsComScraper.extractPhotos extracts up to 5 photos via JSON-LD > OG > carousel
- Listings with no rent are saved (not skipped) with rentMonthly: null
- run.ts outputs `::metrics::` JSON line and exits 1 when 0 listings scraped
- Stale listings (30+ days inactive) archived to listing_history then deleted
- Stealth plugin (playwright-extra + puppeteer-extra-plugin-stealth) integrated
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-01-SUMMARY.md`
</output>
