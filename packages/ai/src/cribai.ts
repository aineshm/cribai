export class CribAI {
  // Phase 5 implementation — streaming AI advisor
  async *chat(_campusId: string, _query: string): AsyncGenerator<string> {
    // TODO: Implement with PageIndex traversal + Claude streaming
    yield 'CribAI is not yet implemented.';
  }
}
